---
'@siracusahq/design-system': minor
---

検索付きマルチセレクト `MultiSelect` を追加。Combobox と同じ cmdk ベースで、開くと検索入力欄で絞り込みながら複数選択できる。選択済みはトリガー内にタグ表示（`maxDisplay` 超過分は「+N」に集約）、リスト項目はブランドカラーのチェックボックス表示で、選択してもメニューは閉じない。
