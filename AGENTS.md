# AGENTS.md — 全AIエージェント共通の運用ルール

このリポジトリで作業する全てのAIコーディングエージェント (Claude Code / Codex / Gemini CLI / OpenCode / Cursor 等) に適用される。パッケージ構成・トークン運用の鉄則・カラー使い分けは `CLAUDE.md` を必ず読むこと。

## Git運用

- **main 上で直接作業・commit・push しない** (main への push は本番デプロイに直結する。`.claude/hooks/guard-main.sh` でもブロックされる)
- 作業ブランチは `feat/` `fix/` `docs/` `chore/` + 短い英語スラッグ (エージェントは `agent/<エージェント名>/<作業内容>` でも可)
- **こまめに commit / push する**。push されるまで作業内容は他の誰からも見えない
- **色・レイアウトを変える PR は、マージ前にデプロイプレビューで確認する** (CLAUDE.md 既定)

## レビュー

- **クロスファミリーレビュー必須**: PR は実装したエージェントと別ファミリーのエージェント (または人間) のレビューを経てから merge する
- レビュー指摘への対応は、同じブランチへの追いコミット + push で行う (PR が自動更新される)
