# GUIDELINES.md — 判断のための規範

`@siracusahq/gtm-design-system` で LP を作るときの Do's & Don'ts。入口は [AGENTS.md](./AGENTS.md)。

**このファイルに書いてあるのは、API では表現できない判断だけ**である。型で落とせるもの・警告で止まるものは書かない（§6 に一覧だけ置く）。すべてのルールに実測の根拠を1行添えてある。根拠を引けないルールは書いていない。

**語彙**: ブランド4 = `corporate` / `polastack` / `peerdesk` / `peerdesk-taxpeer`。トーン3 = `trust` / `product` / `campaign`。ページ型11は §2 に1節ずつある。

実測の原典（本文の数値はすべてここから引いている）:

- `docs/research/research-lp-anatomy.md` — 国内 BtoB 19 ページのアナトミー調査（2026-08-02）
- `docs/research/research-cta-band.md` — 中間 CTA 帯の CSS 実測（国内 11 ページ、2026-08-06）
- `docs/research/research-case-study-detail.md` — 事例記事のフォーマット実測（9 サイト × 3 記事、2026-08-13）
- `packages/ui-web/docs/composition-redesign.md` — 上記から導いた設計方針の正本

---

## 1. 思想（3点）

**① 構成の判断はシステムが持つ。書き手の仕事は埋めること。**
セクションの順序・面と余白のリズム・CTA の配置は、パターン（`defineLandingPage`）とページコンテナ（`<Page>`）が決める。旧 API は 18 セクションが背景 4 通り × 余白 5 通りを独立に受け取っており、10 セクションの LP で 20^10 ≈ 10兆通りの見た目が表現できた。成立するのはそのうち十数通りしかない。**選択肢を減らしたのは制限のためではなく、選ぶ必要をなくすため。**

**② すべてのルールに実測の根拠がある。**
「一般に良いとされる」は採らない。国内 BtoB SaaS の配信 CSS と DOM を直接読んだ 19 ページの実測が根拠であり、実測で反証されたルールは撤回してある（例:「プライマリ CTA はページに2箇所まで」は該当ページ 0 件だったため撤回）。逆に、検証できていないことは「自分たちの決定」と明示している（面のリズムが長らくこれだった。
2026-08 に面の実測が取れたため、LP の面はブランドティント + パターン別の明示割当へ改訂した）。

**③ 守りは機械が担う。人間のレビューは内容に集中する。**
**型エラー → dev 警告 → ページ単位 VRT** の3段。型で落ちるものは文章で二重に禁止しない。レビューで見るべきは「コピーが正しいか」「数値の出典が正しいか」であって、余白や色ではない。

---

## 2. ユースケース別ガイド（ページ型11）

ページ型は**2系統**に分かれる。獲得・訴求の**ランディング型**（`product` / `product-portfolio-top` / `lead-gen` / `corporate-top`）と、キャッチコピー型ヒーローを持たずサイト内を回遊させる**コンテンツ型**（`case-study-list` / `case-study-detail` / `article-list` / `article-detail` / `resources-library` / `seminar-list` / `seminar-detail`）。API はどちらも `defineLandingPage()` で同じだが、Storybook の棚は Patterns/LandingPage・Patterns/CorporatePage・Patterns/ContentPage に分かれている。

**ランディング型（獲得・訴求）**

### `product` — 単一製品の LP

**いつ選ぶ**: 検索・指名流入で「この製品を評価しに来た人」が着地するページ。製品名でランディングし、機能と料金を比較して検討に進む導線。実測で最多の型。

**素材チェックリスト**

- hero: 見出し + リード文 + **オファー1〜2本**（ラベルは §3 の2種ルールに従う）
- contentHub: コンテンツ回遊（任意。実測 9/13。FAQ の後・締めの前に入る）
- proof: **数値3〜4点 + `asOf`（基準時点）**、またはロゴ6社以上のどちらか
- features: **機能3〜6点**（各 title + description）
- showcase: 機能の深掘り（任意）。左右交互のメディア + 箇条書き。王道の実例は Storybook の `製品LP_Polastack_王道`（Polastack 実サイトの「設定ファイルで見る機能一覧」）
- pricing: 料金プラン（任意）
- reasons: 「選ばれる理由」3〜4点（任意）
- cases: 導入事例1〜3件（任意）
- faq: 任意
- closing: 締めの CTA（見出し + オファー。省略不可）

| Do                                                              | 根拠                                                                     |
| --------------------------------------------------------------- | -------------------------------------------------------------------------- |
| 料金を事例より前に置く（パターンが自動でそうする）              | 両方持つページで 料金→事例 が4件、事例→料金 が1件                        |
| FAQ は必要なときだけ足す                                        | 製品系12ページ中5ページ（42%）にしかない。必須セクションではない          |
| 締めの CTA 面を必ず置く                                         | フッター直前の最終 CTA 面は 10/12（83%）                                 |
| 数値には必ず `asOf` を渡す                                      | 実測ではほぼ全社が時点を併記。景品表示法上、時点のない実績表示は不当表示になりうる |

| Don't                                                                 | 根拠                                                                 |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 社会的証明（ロゴ帯・数値訴求）を両方とも省く                          | 19/19 のページが何らかの数値訴求を持つ。例外は1ページも無かった      |
| 機能セクションに10点以上を並べる                                      | 列数は件数から導出される。7件以上は3列に流れて1件あたりの情報が薄くなる |

### `product-portfolio-top` — 複数プロダクトの玄関

**いつ選ぶ**: コンパウンド企業のトップページ。複数の製品を持ち、**カードグリッドで下層の製品 LP へ分岐させる**のが中核。`corporate-top` でも単一 `product` でもない独立した型で、実測 19 ページ中 7 ページ（バクラク / SmartHR / Bill One / カミナシ / ANDPAD / HRBrain / kintone）がこれ。Polastack もピアデスクシリーズも、まずこの型を必要とする。

**素材チェックリスト**

- hero: 会社・シリーズ全体のキャッチ + オファー1〜2本
- proof: 全社合計の数値（導入社数・シェア）+ `asOf`、またはロゴ帯
- products: **製品カード（`brand` / `name` / `description` / 下層 LP への `href`）**。省略不可
- features / cases / midCta: 任意
- closing: 締めの CTA（省略不可）

| Do / Don't                                                                             | 根拠                                                                       |
| ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| **Do**: 製品カードの `brand` に各製品のブランドキーを渡す                              | カードのチップとリンクだけがその製品の色になる。ページ全体の色は親の `brand` のまま |
| **Do**: 数値は「製品単体」ではなく「全社・シリーズ合計」を出す                          | 玄関ページの役割は分岐であって、単一製品の説得ではない                     |
| **Don't**: 特定の1製品の機能詳細をここに書く                                            | 下層 LP の仕事。玄関で書くと分岐の判断材料が埋もれる                       |

### `lead-gen` — 獲得専用 LP（広告着地）

**いつ選ぶ**: **広告から直接着地させるページ**。資料ダウンロードや問い合わせだけを取る。`product` との分岐はここ — 検索・指名流入なら `product`、**広告着地なら `lead-gen`**。

**素材チェックリスト**

- hero: 資料タイトル + 何が分かるか。オファーはフォームへのページ内アンカー1本のみ（0本でもよい）
- contents: **資料の中身の箇条書き**（省略不可）
- stats: 数値訴求（任意だが実測 2/2 が残している）
- form: `ResourceRequestForm` / `ContactForm` / `DemoRequestForm` のいずれか（省略不可）

| Do / Don't                                                              | 根拠                                                                          |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| **Do**: グローバルナビを付けない（型に `header` が無い）                | 実測 2/2 がナビを剥がしている。広告着地でサイト内を回遊させると獲得が漏れる    |
| **Do**: オファーを1つに絞る                                             | 迷わせないための型。`product` と違い分岐の余地を作らない                       |
| **Do**: 数値訴求を残す                                                  | 2/2 がナビを剥がしても数値訴求は残している                                    |
| **Don't**: フォームを省いて別ページへ飛ばす                             | この型の存在意義がフォームの同居にある（別ページ遷移なら `product` でよい）    |

### `corporate-top` — コーポレートトップ

**いつ選ぶ**: 運営会社のトップページ。読み手は投資家・採用候補・パートナーであって、製品の買い手ではない。

**素材チェックリスト**

- hero: **ビジョンステートメント**（製品訴求ではない）。offers は「私たちについて」「採用情報」等
- services: 事業紹介（省略不可）。フラットなカード分岐なら `ServicePortfolio`、
  「事業（小見出し）→ プロダクト（ビジュアル + 導線）」の2階層で見せるなら `BusinessShowcase`
- stats: 事業規模の数値（任意）
- about / closing: 任意

| Do / Don't                                                            | 根拠                                                                         |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **Do**: 数値は事業規模・市場規模にする                                | 実測（LayerX）の数値は市場規模・利回りであって導入実績ではない                |
| **Don't**: コンバージョン CTA を置く                                  | 実測でこの型には資料 DL も問い合わせも無い。導入ロゴも 0 社                   |
| **Don't**: 製品スクリーンショットを主役に置く                         | 訴求対象が買い手ではないため、製品ビジュアルが要らない                       |

**コンテンツ型（一覧・記事・詳細で回遊させる）**

### `case-study-list` — 導入事例の一覧

**いつ選ぶ**: 事例を**一覧で見せて絞り込ませる**ページ。個別の事例記事ではない。

**素材チェックリスト**

- page: **短いページタイトル**（キャッチコピー型のヒーローは使わない）+ 説明文
- list: 事例カード（`pickup` / `filterAxes` / `pageSize` は任意）
- closing: 末尾 CTA（任意。ヒーローが無くオファーを再利用できないため `actions` は必須）

| Do / Don't                                                                          | 根拠                                                                     |
| ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| **Do**: フィルタ軸は「サービス / 業種 / 従業員規模 / 課題」から選ぶ                 | 実測 2/2（SmartHR・バクラク）がこの4軸で共通していた                    |
| **Don't**: キャッチコピー型のヒーローを付ける                                       | 実測 2/2 とも短いページタイトルから始まる。この型だけ hero を持たない     |
| **Don't**: 個別事例記事をこの型で作る                                               | 個別記事は `case-study-detail`（下の節）の仕事。一覧と記事はメタ情報の型を共有する |

### `case-study-detail` — 個別の事例記事

**いつ選ぶ**: 事例1件を記事として読ませるページ。`case-study-list` の遷移先。**この型は「量産される記事」のためにある** — 実測した 9 サイト × 3 記事のサイト内一貫性は **97.6%**（63 項目中 61.5 一致）で、記事ごとに変わるのは章数・文字数・写真枚数という「量」だけだった。構成を1本ずつ判断させてはいけない。

**素材チェックリスト**

- article: **成果か課題を含むタイトル**（会社名だけのタイトルは実測 1/9 の例外）+ 一覧への `backTo` + ヒーロー写真（任意・alt 必須）+ リード文 + 公開日（任意）
- profile: **会社プロフィール**（社名 / 業種 / 従業員規模 / 利用サービス / 課題）。省略不可
- speakers: 話者1〜4名（任意）
- summary: 冒頭サマリー（`課題` 必須 / `決め手` 任意 / `効果` 必須。任意）
- chapters: **本文の章。最低2章**（中央値5章）。各章は地の文（`paragraphs`）か問答（`qa`）のどちらか + 写真1枚まで
- related: **関連事例2件以上**（3件が最頻）。一覧カードと同じデータをそのまま渡す
- closing: 末尾 CTA（ヒーローが無くオファーを再利用できないため `actions` は必須）

| Do / Don't                                                                     | 根拠                                                                                     |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **Do**: タイトルに成果か課題を入れる（「申請1件30分→5分へ」）                  | 成果・課題を含むキャッチ型が 8/9。数値は**タイトルと章見出しの文章の中**にある            |
| **Do**: 一覧カードと同じ1件のデータからプロフィールを作る                      | メタ情報の軸（業種 / 従業員規模 / サービス / 課題）は 9/9 サイトで一致。型も共有している |
| **Do**: 写真には人物と文脈を書いた `alt` を付ける                              | 意味のある `alt` を書いていたのは 9 サイト中 **1 サイト**だけ。規範はここで効く          |
| **Don't**: 冒頭に数値タイル（大きい数字の並び）を置く                          | 記事上部の大きな数値は 27 記事中 **0 件**。数値バッジは一覧カードの語彙                  |
| **Don't**: 引用の飾り枠を作る                                                  | `blockquote` は 27 記事中 **0 件**。発言は問答か写真キャプションの話者名で示す            |
| **Don't**: 目次を付ける・章ごとに面を変える                                    | 目次は 2/9。本文を単一の面に置くのは **9/9**（面リズムは記事の外側だけで働く）           |

### `article-list` — お知らせ / ブログの一覧

**いつ選ぶ**: プレスリリースやコラムを**一覧で見せて絞り込ませる**ページ。事例の一覧ではない。

**素材チェックリスト**

- page: **短いページタイトル**（キャッチコピー型のヒーローは使わない）+ 説明文
- list: 記事カード（`filterAxes` / `pageSize` は任意）
- closing: 末尾 CTA（**任意**）

| Do / Don't                                                                | 根拠                                                                              |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **Do**: 日付は ISO（`YYYY-MM-DD`）で渡す。表示書式はシステムが決める      | 実測が4通りに割れている（`2026.07.07` / `2026-08-14` / `2025/12/19` / `2026年07月14日`） |
| **Do**: News とブログで同じ型を使う                                        | 一覧の構造差はサムネイルの有無だけ（News 3/6・ブログ 5/7）。多数派のカードに寄せた |
| **Don't**: 末尾 CTA を必須と考える                                         | News 7/12・ブログ 11/15。SmartHR ニュースは 0/3（事例一覧とは違う）              |
| **Don't**: 並べ替え UI・検索ボックス・無限スクロールを足す                 | 並べ替え 1/14・検索 2/14・無限スクロール **0/14**                                |

### `article-detail` — 個別の記事

**いつ選ぶ**: News かブログの記事1本。`article-list` の遷移先。

**素材チェックリスト**

- article: `kind`（`news` / `blog`）+ タイトル + 公開日 + 本文の章（最低1章）。戻り導線・カテゴリ・写真・リード文・シェアは任意
- article（`blog` のみ）: 著者 / 監修者 / 目次 / 更新日
- related: 関連記事（任意）
- closing: 末尾 CTA（**任意**）

| Do / Don't                                                              | 根拠                                                                    |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| **Do**: `kind` で News とブログを分ける                                 | 著者 0/12・目次 0/12・更新日 0/12。両方 optional の1型にすると実測に無い構成が型で通る |
| **Do**: 公開日を必ず出す                                                | News 12/12・ブログ 15/15（事例記事の 6/27 とは逆転している）           |
| **Don't**: 冒頭に数値タイルを置く                                       | **0/54**（事例 0/27 + News 0/12 + ブログ 0/15）                        |
| **Don't**: 記事内サイドバー（人気記事・フィルタ）を作る                 | ブログ 3/7・News 0/7。一覧の仕事                                        |
| **Don't**: 章ごとに面を変える                                           | 記事本文は単一の面（`case-study-detail` と同じ）                       |

### `resources-library` — 資料ライブラリ

**いつ選ぶ**: ホワイトペーパー・事例集などの資料を並べて配るページ。

**素材チェックリスト**

- page: **短いページタイトル** + 説明文
- list: 資料カード（`pickup` は任意）

| Do / Don't                                                        | 根拠                                                          |
| ------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **Do**: カードの遷移先は詳細ページでもフォームでもよい            | 実測に**両方が存在する**（詳細ページ経由7サイト / フォーム直行 Sansan・Bill One） |
| **Don't**: 日付を出す                                             | **0/7**（記事一覧は 14/14 が持つ。資料は日付を持たない）       |
| **Don't**: 形式表記（PDF / ページ数）を出す                       | 一覧 0/7 + 個票 0/6 = **0/13**                                |
| **Don't**: ページャ・検索ボックス・並べ替え UI を足す             | 資料 0/7。無限スクロールも 0/31                               |
| **Don't**: 資料個票のページ型を探す                               | 個票は `lead-gen` に `header` を渡して組む（差分が `header` 1点のみ） |

### `seminar-list` — セミナー一覧

**いつ選ぶ**: 開催予定・終了・アーカイブのセミナーを一覧で見せるページ。

**素材チェックリスト**

- page: **短いページタイトル** + 説明文
- list: `status` 付きで**全件**（グルーピングはパターンが決める）

| Do / Don't                                                     | 根拠                                                        |
| ------------------------------------------------------------------ | -------------------------------------------------------------- |
| **Do**: 開催予定と終了を同じ一覧に混ぜて渡す                  | 実測 **0/8**。8サイトすべてが同じ一覧の中で両方を扱う      |
| **Do**: 状態は必ず文言で示す（色だけで分けない）              | 「受付中」と「受付終了」を色相だけで分けると色覚特性で判別できない |
| **Don't**: 予定用と終了用でページを分ける                     | 実測 **0/8**                                                |
| **Don't**: 状態の見せ方（タブ / フィルタ / バッジ）を選ばせる | 実測がタブ 4/8・フィルタ 1/8・バッジ 1/8 に割れており、選ばせる根拠が無い |

### `seminar-detail` — セミナー詳細

**いつ選ぶ**: セミナー1本の申込ページ。`seminar-list` の遷移先。

**素材チェックリスト**

- seminar: `status`（`upcoming` / `closed` / `archive`）+ タイトル + 概要 + おすすめ + プログラム + 開催要項 + 登壇者 + **申込フォーム**

| Do / Don't                                                    | 根拠                                                             |
| ----------------------------------------------------------------- | ------------------------------------------------------------------- |
| **Do**: `status` で分ける                                     | アーカイブに開催日時は無く、開催予定に視聴期限は無い。全部 optional にすると実測に無い構成が型で通る |
| **Don't**: 末尾 CTA を置く                                    | **0/21**。フォーム自体が CTA                                     |
| **Don't**: 関連コンテンツ・SNS シェアを置く                   | 関連 1/21・シェア **0/21**。フォームの手前で他ページへ逃がさない |
| **Don't**: 有料・満席・ハイブリッドを表現しようとする         | 参加費は全件無料・`sold-out` 0/29・`hybrid` 0/21                |

---

## 3. 横断の Do's & Don'ts

### CTA

| ルール                                                                       | 根拠                                                                                    |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| **プライマリ CTA のラベルは2種類まで。反復は自由**（目安4〜6回）             | 総 CTA 数の中央値は 15〜16 本で「2箇所まで」のページは 0 件。一方ラベル種類は 2 種が最頻 |
| **ファーストビューのオファーは2本**                                          | FV 2本が 13/17（76%）。型でも最大2本に制約している                                     |
| 2本の組み方は「軽+重」でも「軽+軽」でもよい                                  | 軽+重 が7件、**軽+軽（資料DL 2本）が4件**。後者は軽重ではなく「何の資料か」の分岐       |
| ラベルは行動を書く（「送信」ではなく「資料をダウンロード」）                 | 送信ボタンのラベルをオファー名と一致させるのが実測の作法                                |
| kicker（`＼5分でわかる資料をプレゼント／` 等）で重さを下げてよい             | 実測で7種確認。「ご相談だけでもOK お問い合わせ」のように重い CTA を和らげる慣行がある   |
| **面を持つ CTA 帯（`CTABand`）の反復は中間1〜2回まで**                       | 面を持つ CTA 帯を3回以上反復しているサイトは 0 件。4〜6回の反復は面を持たない CTA の領分 |

### 面と余白

| ルール                                                     | 根拠                                                                                       |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| **セクションの境界に線を引かない**                         | セクション面を上下の線で区切る国内 BtoB SaaS は 11 ページの CSS 走査で **0 件**            |
| 境界は「面の色差」だけで作る                               | 淡い面（対比 1.12:1）を線なしで成立させている実例がある。手段は余白の確保と中身の濃さ       |
| **暗い面を3つ連続させない**                               | 可読性の問題。他社実態ではなく自分たちの確定規則                                            |
| **面の割当は `<Page>` かパターンが決める。セクションに面の props は無い** | 割当先は2つ。既定は `<Page>` の自動ゼブラ（ニュートラル）、LP 系のページ型は `defineLandingPage` のパターンが `surfaces` で明示する |
| **顔になるページの面は「白 × ブランドティント淡色」**（`tinted`）。ニュートラルグレーの交互は事例系・記事系・獲得系に残す | 実測 8 サイト中 5 サイトが淡い有彩色面（SmartHR `#f4f8f9` = 1.069:1 / マネーフォワード `#f2f5ff` = 1.089:1 / Chatwork `#f7f1e7` = 1.124:1 / kintone / バクラク、加えてカミナシ `#e8f2ff`。docs/research/research-eyebrow.md §4-3） |
| **リズムの決め方は型で違う。LP 系は「白の連続 + 塊で浮かす」（`surfaces`）、`corporate-top` は「交互のまま色だけティント」（`autoSurface`）** | LP は買い手が読むので社会的証明を浮かせる意味がある。コーポレートトップは読み手が買い手ではなく、どの節も等価に並ぶため交互が合う。任意スロットの有無で崩れない `autoSurface` を使う |
| **面を機械的に ABAB で交替させない**。白を続け、社会的証明のような塊だけを浮かせる | 機械的ゼブラの実サイトは実測で確認できていない。交替回数は 1〜3 回が主流で、白ベース型（面をほぼ使わない）も 3 例 |
| 中間 CTA 帯（`CTABand`）の直前に `muted` を置かない        | 沈んだ面（`#f4f4f5`）と淡いブランド面の対比は 1.053:1 で面差が知覚できない（dev 警告が鳴る）。`tinted` なら 1.06:1 以上を確保できるので隣接してよい |
| **余白は「片側の値」ではなく「継ぎ目の実効余白（前の `padding-bottom` + 次の `padding-top`）」で判断する** | 実測（国内13サイト）の継ぎ目は中央値 96〜104px・最大 174px。片側の値と突き合わせると倍の余白を正当化してしまう（実際に起きた。research-eyebrow.md §4-2 追記） |
| **ファーストビューはヒーロー（＋その章の証明帯）で持つ。高さはパディングではなく `--hero-min-height`（視野基準）で担保する** | 次の章の見出しがファーストビューに入っているサイトは 13件中2件。パディングで埋めているサイトは 0件（コピー量と画面高で結果が変わるため）。campaign トーンだけは適用外（docs/research/research-first-view.md） |
| **セクション見出しの区切りは飾り線（既定・自動）**。eyebrow は見出しから内容が読み取れないときだけ・日本語で | ピル型ラベルは 41 セクション実測で 0 件（ピル型は CTA の肩コピーの記号）。飾り線採用サイトは適用率ほぼ100%（docs/research/research-eyebrow.md） |

### ロゴと数値

| ルール                                                                          | 根拠                                                                                 |
| --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| **社会的証明はロゴ帯か数値訴求のどちらかを必ず置く**                            | 19/19 のページが数値訴求を持ち、両方無いページは 0 件                                |
| **ロゴは 0 社に振り切るか、6 社以上出す**                                       | ロゴ 0 社（数値に振り切る）が 6/19（32%）。1〜5社は日本語ページに実例 0 件           |
| 少数しか出せないなら、ロゴ帯にせず事例カードに紐付ける                          | SmartHR 6社 / Bill One 8社が事例セクション内に置く形を採っている                     |
| ロゴを載せるなら中央値は約20社。14社以上は自動でカルーセルになる                | 掲載時の中央値 ≒ 20社、14社以上の5ページはいずれもカルーセル                         |
| **数値には `asOf`（基準時点）を必ず添える**                                     | 景品表示法。時点の分からない No.1 表示・実績表示は不当表示になりうる                 |
| **事例カードにはインタビュー写真を付ける（photo・alt 必須）。写真つきでは引用を1〜2文に絞る** | 写真は実在性の証拠（alt には人物と文脈を書く）。比率 16:9 とトリミングはシステムが正規化するので任意の写真を渡してよい（2026-08-11 ブランド決定。自社実測は未取得 — SmartHR・バクラクの事例ページの実態と一致） |
| 数値は「導入規模」「No.1・シェア」「継続率」「削減率」の4型から選ぶ             | 19 ページの数値訴求はこの4型に整理でき、規模+順位の同時掲出が最頻（10/19）           |

### フォーム

| ルール                                                              | 根拠                                                                                  |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| **用途ごとにフォームを分ける**（問い合わせ / 資料請求 / デモ）      | SmartHR 3本・Bill One 2本・freee人事労務 3本。1フォームに用途セレクタを付ける社は無い |
| 送信ボタンのラベルをオファー名に一致させる                          | 「送信」等の汎用語は実測の作法から外れる（dev 警告の対象）                            |
| `product` 系ではフォームを別ページに置いてよい                      | 15/19（79%）が別ページ。埋め込みは `lead-gen` の主役であって product の必須部品ではない |
| **言語は既定 `ja`。英語にするときだけ `lang="en"` を渡す**          | 静的出力（SSG）でブラウザの `document` を覗けないため、明示指定でしか決定できない     |
| **項目を足すときは `extraFields`。見た目の口は開いていない**        | 追加できるのは「項目」であって「見た目」ではない（2026-08-15 ブランド決定）。`extraFields` はデータだけを受け取り、描画は DS のフォーム部品に固定される。`className` も `children` もカスタムレンダラも無い |
| **問い合わせ種別・電話番号・個人情報同意は名前付きの props**（`inquiryTypes` / `phone` / `consent`） | 国内 BtoB の定型3項目。`consent` は未チェックで送信できない（同意は任意にできる性質のものではない） |
| **会社名の要否は `company` prop**（off / optional / required。既定 required） | コーポレートサイトの問い合わせ正本は会社名を任意と定める（2026-08-16 申し送り P4）。`ContactForm` のみ — 資料請求・デモ予約は BtoB リード獲得の器なので必須のまま |
| **外部の入力補完サービスは既定でオフ**（`ichisanEnabled`）          | 外部スクリプトを読む＝送信先が1つ増える判断であり、利用側が明示的に行うべきもの（0.12.0 で既定を反転） |

### 色

| ルール                                                                        | 根拠                                                                    |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| **`primary`（`#008575`）= 操作用**。ボタン背景・リンク・フォーカスリング      | 白文字 4.55:1 で WCAG AA 適合                                          |
| **`brand`（`#13c3a0`）= 装飾用**。グラデーション・グロー・ダーク背景上のみ    | 白背景での対比は 2.25:1 しかない                                       |
| **明背景のテキスト・ボタン背景に `brand` を使わない**                         | 同上。CI（`src/tokens/tokens.test.ts`）がコントラスト比を実計算して落とす |
| CTA は別色でもよい（`--color-bg-cta` スロット。未指定なら操作色にフォールバック） | 実測でブランド主色が青（`#0360e5`）でも CTA の塗りが黄（`#ffdb22`）の社がある |

### コンテンツ回遊（`contentHub`）

| ルール                                                                 | 根拠                                                              |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| **系統ごとに塊を作る。複数系統を1つのグリッドに混ぜない**              | 実測 **0/11**。混在させているサイトは1つも無い                   |
| **「これは資料です」という種別ラベルを付けない**                       | 実測 **0/17 枠**                                                  |
| **お知らせは日付つきの行リスト、それ以外はサムネイル付きカード**       | サムネイル保有は 資料/セミナー/コラム 9/10 に対し **News 0/7**。例外なし |
| **タブ・フィルタを付けない**                                          | 実測 **0/11 ページ・0/17 枠**。絞り込みは一覧ページ型の仕事       |
| 濃色にできるのは入口タイルだけ                                        | アイテムカードを濃色面に置いた例は **0**。濃色として観測されたのは大型カードと帯 |
| カードは1系統3件が目安                                                | 件数の中央値 3（最頻 3 = 9/17）。News のみ中央値 4                |
| 見出しと一覧導線の文言は必ず渡す                                      | 導線の文言は **12通りで全部違う**。見出しも News 以外は割れる      |

### 和文コピー

| ルール                                                              | 根拠                                                                       |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| **見出しに `<br>` を手で入れない**                                  | 改行位置はシステムが `text-wrap: balance` で処理する。手動改行は文言差し替えで崩れる |
| 負の字送り（`letter-spacing` のマイナス）を持ち込まない             | 和文で負の字送りを使っている国内 BtoB 企業は 8社中 **0 社**                |
| 本文に 14px 未満を使わない                                          | デジタル庁 DADS が「原則として許容されない」と明記                          |
| **セクション内の散文は見出し1文だけ。説明は箇条書き（最大3点）と数値に分解する** | AirPocketFeature の再設計で確立（2026-08-11 ブランド決定）。散文の段落スロットは廃止済み — 説明を書きたくなったら「要点3つに割れないか」「数値にできないか」を先に考える |
| **この散文の規則は「説明セクション」に適用する。読み物の面（記事・ミッション・代表挨拶）は対象外** | 適用範囲の明文化（2026-08-15 ブランド決定）。事例記事は 2026-08-11 の時点で既に段落を持っている（`chapters[].paragraphs`。実測 9サイト×3記事）。**箇条書きに割れない文章は `ProseSection`、Markdown 由来の本文は `DocumentArticle` の領分**で、機能説明をそこへ流し込むのは規則の迂回にあたる（`ProseSection` は段落5つ以上で dev 警告を出す） |

---

## 4. なぜ選べないのか（期待値調整）

このシステムには、他のライブラリなら普通にある口が意図的に無い。

| 無いもの                                     | 代わりに                                                           |
| ---------------------------------------------- | -------------------------------------------------------------------- |
| `className`                                  | 見た目はトークンとトーンが決める                                   |
| セクションの順序を変える口                   | ページ型（`pattern`）を選び直す                                    |
| 背景・余白を指定する props                   | `<Page>` が前後関係から割り当てる（面をどこで変えるかはページ型ごとにパターンが持つ） |
| 列数・カードの見た目を指定する props         | 件数と位置から導出される                                           |
| 自由な色指定                                 | `brand` と `tone` の組み合わせで表現する                           |

**理由は1つ。品質の標準化がデザインシステムの存在意義そのものだから。** 逃げ道を1つ残すと、そこが最短経路になる。旧 API は `className="bg-neutral-900 py-40"` ですべての規約を迂回でき、しかもそれは静かに起きていた。

**ただし「見た目を選べない」と「内容を足せない」は別の話。** ここで閉じているのは見た目の制御であって、業務上の項目まで禁じているわけではない。フォームの `extraFields` はその線引きの実例で、受け取るのはデータ（項目名・ラベル・種類）だけ、描画は DS のフォーム部品に固定されている。`className` も `children` もカスタムレンダラも無いので、追加した項目も他の項目と同じ見た目になる（2026-08-15 ブランド決定）。

**本当に足りないときの正しい経路**は、逃げ道を探すことではなく **DS 側に部品またはパターンを追加すること**（このリポジトリへの PR）。追加には「なぜ必要か」と一緒に**実測または明示的なブランド判断**が要る。根拠のない選択肢を増やすと、10兆通りに戻る。

なお「選択肢を消す」判断は実際に何度も撤回している（ロゴ6社未満の警告、CTA 2箇所ルール、CTABand の上下線）。**根拠が出れば規範は変わる。** 変えたいときは根拠を持ってくること。

---

## 5. Astro での使い方

```ts
import '@siracusahq/gtm-design-system/styles.css';
```

1行だけ。PostCSS も Tailwind も設定しない。**この1行で足りることは CI の Astro 消費側ビルドで実測している**（`packages/astro-consumer-test`）。

**`.astro` にセクションを直接並べてはいけない。**
`<Page>` の面リズムは `React.Children` で子を1件ずつ見る実装であり、Astro は子を「レンダリング済み HTML の塊」として渡すため、`Page` から見た子が不透明な1件になる。実測では `.astro` に5セクションを直接並べたページで muted 面の出現が **0 件**（`defineLandingPage` 経由は1件）。同じ理由で暗面3連続・h1 重複・CTA ラベルの各警告も働かない。**リズムも規範ガードも外れたただの部品置き場**になる。

推奨形は2つだけ。

1. `defineLandingPage()` + `<LandingPage>`（LP 量産の本線）
2. セクションの並びを `.tsx` のコンポーネントにまとめ、`.astro` からはそれを1つ置く

**対話部品には `client:visible` を付ける。** FAQ のアコーディオン、事例一覧の多軸フィルタ、数値のカウントアップは JS が要る。静的なセクションには付けない（送るバンドルが増えるだけ）。

**フォームは静的出力のまま置く。** Netlify Forms はデプロイ時に静的 HTML の生コントロールを解析して受け口を作るため、`client:*` を付けると検出されない。フォームの言語は既定 `ja` で、明示的に `lang="en"` を渡したときだけ英語になる。

---

### Storybook に載るもの・載らないもの

Storybook は **ビジュアルデザインの採用判断の場**（人間と AI の共通認識づくり）。
載る基準は2つ: (1) ビジュアルが明確にわかり採用可否を判断できる
(2) 例示の役割が他と被らない。構造でガードされているもの（型エラー・dev 警告・VRT）は
ストーリーにせず、本ガイドラインの §6 だけが説明する（2026-08-10 ブランド決定）。

## 6. 守られていることの一覧（読むだけでよい）

**以下は文章を読まなくても機械が止める。** 覚える必要はなく、止まったときに理由が分かればよい。

### 型エラーで落ちるもの

- `className` を渡す（公開インターフェース全件で `Omit<…, 'className'>`）
- 背景・余白・列数・eyebrow のスタイルなど、見た目を選ぶ props を渡す（存在しない）
- ファーストビューのオファーを3本渡す（`offers` は最大2要素のタプル）
- ページ型ごとの必須スロットを欠く（`product` の `features` / `closing`、`lead-gen` の `contents` / `form` など）
- `brand` / `tone` / `pattern` に一覧外の文字列を渡す
- `lead-gen` に `header` を渡す（型に無い）

### dev ビルドでコンソール警告が出るもの（14種）

| # | 警告                                              | 出る場所        |
| - | ------------------------------------------------- | --------------- |
| 1 | ページ内に `h1` が2つ以上ある                     | `Page`          |
| 2 | 暗い面のセクションが3つ連続している               | `Page`          |
| 3 | 強調面（`CTABand` 等）が3つ以上ある               | `Page`          |
| 4 | プライマリ CTA のラベルが3種類以上ある            | `Page`          |
| 5 | ファーストビューの CTA が3本以上ある              | `HeroSection`   |
| 6 | ロゴが1〜5社しかない                              | `LogoCloud`     |
| 7 | 実績数値に `asOf` も `note` も無い                | `StatsSection`  |
| 8 | 社会的証明スロット（`proof`）が空                 | `LandingPage`   |
| 9 | フォームの送信ラベルが汎用語（「送信」「submit」） | フォーム3種    |
| 10 | 散文の段落が5つ以上ある（説明を流し込んでいる疑い） | `ProseSection` |
| 11 | `extraFields` の `name` が組み込み項目と衝突している | フォーム3種    |
| 12 | 面を自分で塗るセクションに `surfaces` で面を指定した（無視される） | `Page`          |
| 13 | 強調面（`CTABand` 等）の直前に `muted` を指定した（面差 1.053:1） | `Page`          |
| 14 | `company="off"` と `ichisanEnabled` を併用した（補完元の会社名欄が無い） | `ContactForm`   |

警告は本番ビルドでは実行されない。Storybook の `Patterns/規範ガード` で、#1〜#9 が実際に鳴る様子を確認できる（#10・#11 はフォームと読み物の部品にあり、それぞれのストーリーで確認できる。#12・#13 は `surfaces` の誤用に対する警告で、`page.test.tsx` がコンソール出力まで検証している）。

### そのほか

- **ページ単位のビジュアル回帰テスト**（Playwright、desktop 1280×800 / mobile 390×844）。和文の最悪ケース（異常に長い / 異常に短い / 英語）を含むページ全体のスナップショットが CI で比較される
- **配布物の検証**: SSR での描画、CSS Modules のマッピング、Netlify Forms 属性、Astro での実ビルド出力を CI が毎回確認している
- **色の検証**: `brand` の誤用（明背景のテキスト・ボタン背景）と WCAG コントラスト比を CI が実計算して落とす
