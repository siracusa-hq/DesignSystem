/**
 * ブランドレジストリ（プロトタイプ）
 *
 * ブランドを増やす = ここに 1 エントリ足すだけ。
 * 色相を1つ書くのがブランド側の意思決定、それ以外は全部生成される。
 *
 * mode:
 *   'generated' … hue から生成器がランプを作る（既定。新規ブランドは必ずこちら）
 *   'explicit'  … hex を明示登録する。既存資産との後方互換のためだけの逃げ道で、
 *                 コーポレート以外に増やしてはならない（テストで本数を固定する）
 */

export const registry = {
  corporate: {
    label: 'コーポレート',
    mode: 'explicit',
    reason:
      '現行 @polastack/tokens の primary スケールが既に公開済みで、下流の実装が hex に依存している。' +
      'H=180 生成値との差は平均 ΔE2000 1.86（最大 5.33）で、生成値に寄せると既存 UI の見え方が変わる。',
    ramp: {
      50: '#e0f2f1', 100: '#b2dfdb', 200: '#80cbc4', 300: '#4db6ac', 400: '#26a69a',
      500: '#008575', 600: '#007567', 700: '#006055', 800: '#004c43',
      900: '#003831', 950: '#00231f',
    },
  },

  // 以下 2 件は色相未定。ブランド側の決定待ちのプレースホルダ。
  polastack: {
    label: 'Polastack',
    mode: 'generated',
    hue: 250, // 要確認: 未定。ダーク面主体・技術者向けという要件に対する仮値
    provisional: true,
  },

  peerdesk: {
    label: 'ピアデスクシリーズ',
    mode: 'generated',
    hue: 25, // 要確認: 未定。非技術者向けという要件に対する仮値
    provisional: true,
    products: {
      // 製品はシリーズ色を既定値として継承する。
      // 専用色が決まったら hue を書くだけで上書き CSS が生成される。
      'peerdesk-core': { label: 'ピアデスク（本体）', hue: null },
    },
  },
};
