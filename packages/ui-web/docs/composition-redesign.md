# Web/LP デザインシステム 再設計方針

`@siracusahq/gtm-design-system` を「コンポーネントの詰め合わせ」から
**「AIエージェントが高品質なLPを高速に生成できる装置」** に作り替えるための設計文書。

## 実測の出典表記

本文中の数値には出典を短く添える。いずれも 2026-08-02 の一次調査。

| 記号   | 調査                         | 内容                                                                          |
| ------ | ---------------------------- | ----------------------------------------------------------------------------- |
| `[LP]` | LP アナトミー調査            | 国内BtoB 19ページのセクション順・CTA・ロゴ・数値訴求・フォーム・追従CTAの実測 |
| `[MB]` | マルチブランド調査           | 国内コンパウンド6社のCSS実測 + L\*C\*h 計算                                   |
| `[VL]` | ビジュアル言語・和文組版調査 | 8社の生CSS実測 + KV画像の目視確認 + デジタル庁DADS                            |
| `[NB]` | News / ブログ調査             | 国内BtoB 9社の一覧16ページ + 記事27本の構成実測（2026-08-14）                 |
| `[RS]` | 資料 / セミナー調査           | 国内BtoB 8社の資料一覧7 + 資料個票6 + セミナー一覧8 + セミナー詳細21の構成実測（2026-08-14） |

**`[NB]` と `[RS]` だけ手法が違う。CSS実値（余白・フォントサイズ・読み幅・色）を含まない。**
環境の制約により実ブラウザ計測ができず、`WebFetch` による構造の読み取りで代替したため、
主張できるのは構成・情報設計だけである。書式が要る箇所は `case-study-detail` の実装値を流用すること。

出典のない数値は本リポジトリ内の実測(ストーリー内の prop 使用回数、消費側の再現環境など)。
調査記録の全文はリポジトリの `docs/research/` に収録している。

---

## なぜやるか

現状のパッケージは、業務システムUI（`@siracusahq/design-system`）のスタックを
ほぼそのまま複製している。目的が違うのに構造が同じであることが、
以下の問題を同時に生んでいる。

### 問題1: 配布物が完成品になっていない

Tailwind CSS v4 は**利用者のソースを走査**してCSSを生成する。`node_modules` は既定で
走査対象外であり、配布CSSにも README にも `@source` の記述がない。

最小の消費側アプリで実測した結果:

| 消費側の設定                                            | 出力CSS | 生成ユーティリティ |
| ------------------------------------------------------- | ------- | ------------------ |
| `@import '@polastack/gtm-design-system/theme.css'` のみ | 9.15 kB | **0件**            |
| 上記 + `@source '.../dist'`                             | 67.8 kB | 全件               |

つまり**当時公開されていた 0.2.0（旧 `@polastack` スコープ）は、素直に導入すると無スタイルでレンダリングされる状態だった**
（README への手順明記は Phase 10 で対応済み。恒久対応が Stage 2 の CSS Modules 化）。

同じ構造の問題がフォントにもある。`--font-sans: 'Inter', 'Noto Sans JP'` は宣言
されているが、実際に読み込んでいるのは `.storybook/preview-head.html` だけで、
これは配布物に含まれない。

どちらも **Storybook では完璧に見え、CI（Build / Typecheck / Test / Size）でも
検出されない。** `pnpm size` は JS しか測っていない。

### 問題2: APIが統一感を生まず、ばらつきを生んでいる

18セクションすべてが `background`（4通り）と `spacing`（5通り）を独立に受け取る。
10セクションのLPを組むと、背景と余白の組み合わせだけで **20^10 ≈ 10兆通り**。
このうち成立するのは十数通りしかない。

AIエージェントは props を見つければ使い、しかもLPごとに違う選び方をする。
**セクションが自分の見た目を決める権限を持っていること**が、統一感が出ない原因。

さらに、使われていない選択肢が大量に残っている（ストーリー内の使用回数）:

| prop             | 選択肢数           | 使用回数                      |
| ---------------- | ------------------ | ----------------------------- |
| `eyebrowStyle`   | 6（×15セクション） | **0**                         |
| `cardStyle`      | 2                  | **0**                         |
| `backgroundMesh` | bool               | **0**                         |
| `animated`       | bool               | **0**                         |
| `scrolling`      | bool               | **0**                         |
| `logoStrip`      | node               | **0**                         |
| `titleGradient`  | string             | **0**（かつ実装が破綻。後述） |

### 問題3: 意匠が業務システム由来のまま

- `radii` は共通トークンで最大 1.5rem。実使用は `rounded-full` 15回 / `rounded-2xl` 9回と
  **スケールの上端に張り付いている**
- `shadows` はUI操作用の小さい影のみ。LPの「浮いたカード」に使える語彙がない
- `duration` は最大500ms、`easing` は Material 由来のみ。結果 `AnimateOnScroll` が
  `duration = 600` / `ease-out` を**ハードコードしてトークンを迂回している**
- `tracking-[-0.04em]` / line-height 1.11 は欧文ディスプレイ前提。
  **和文で負の字送りを使っている国内BtoB企業は8社中0社**（`[VL]`）。詰めは `letter-spacing`
  ではなく `palt` で行うのが共通の作法であり、現行実装はこの作法に反している

### 問題4: ブランドが3つに増えるのに、色が1系統しかない

このシステムは今後、**コーポレート / Polastack / ピアデスクシリーズ**の3ブランドを支える。
ピアデスクシリーズは配下に複数製品が将来増える。

現状のトークンは単一ブランド前提で、`primary` と `brand` の2色が hex 直値で固定されている。
ブランドを増やすたびにトークンを増やす作りでは、**バクラクが実際に踏んだ破れ方**
（プロダクト別CSSでの生hex直書き、他プロダクトのランプを参照したコピペ残り、
primary のランプ位置が `-60` と `-70` で混在）を確実に再現する（`[MB]`）。

### 問題5: ビジュアル資産の語彙がゼロ

18セクションのうち画像を扱う口は `HeroSection.image` と `FeatureShowcase.image` の
2つだけで、いずれも `React.ReactNode` の素通し。実装コードに `<img>` は0件、
`aspect-*` も0件。**画像の縦横比・枠・影・傾き・プレースホルダについて、
このシステムは何のルールも持っていない。**

一方、調査した8社のヒーローは**全社が何らかの主役ビジュアルを持ち、
その処理手法（枠の有無・正対か傾きか・読ませるか沈めるか）がブランドの識別子になっている**（`[VL]`）。
ここが空白のままでは、AIが生成したLPは「文字だけのページ」か
「毎回違う扱いの画像が並ぶページ」のどちらかにしかならない。

---

## 方針 — 6つの Stage

| Stage | 内容                                                                  | 効く問題     |
| ----- | --------------------------------------------------------------------- | ------------ |
| 1     | テーマ契約とトークン（マルチブランド / 和文タイポ / 意匠）            | 問題3・問題4 |
| 2     | 全コンポーネント1周（ビジュアル資産 + props削除 + CSS Modules）       | 問題1・2・5  |
| 3     | ページ層（`<Page brand tone>` / `defineLandingPage()` / ページ型5種） | 問題2・速度  |
| 4     | リード獲得（計測 / 追従CTA / 2オファー / フォーム摩擦）               | 事業成果     |
| 5     | 守り（ページ単位VRT / 和文最悪ケース / dev警告 / 結合テスト）         | 全部         |
| 6     | AI向け規範ファイルの同梱                                              | 速度・再現性 |

Stage 2 で CSS Modules へ移行する（後述の案C）ことが、Stage 1・3 を
**「守られるルール」にするための土台**である。
Tailwind クラスを配布したままだと、`className="bg-neutral-900 py-40"` で
すべての規約が迂回でき、しかもそれは静かに起きる。

Stage 2 を「props削除」「CSS Modules 移行」「ビジュアル資産」の3周に分けない理由は、
**同じファイルを3回書き換えることになるから**。1コンポーネントにつき1回だけ触る。

---

## Stage 1: テーマ契約とトークン

### 1-1. マルチブランド体系

支える単位は3階層。

| 階層               | 単位                                         | 買い手                       | アクセント色相                     |
| ------------------ | -------------------------------------------- | ---------------------------- | ---------------------------------- |
| コーポレート       | 運営会社（Siracusa）のコーポレートサイト     | 投資家・採用候補・パートナー | **ティール（`#008575` アンカー）** |
| プロダクトブランド | Polastack（Enterprise Agent Stack）          | **技術者**                   | **未定**                           |
| シリーズブランド   | ピアデスクシリーズ（配下に製品が将来増える） | **非技術者**                 | **未定**                           |

ピアデスクの英字表記は **`peerdesk`** で確定（2026-08-03 ブランド側決定）。
npm スコープは `@polastack/*` から **`@siracusahq/*`** へ変更することが同日決定した（npm 上で `siracusa` スコープは取得不可だったため `siracusahq` を確保）
（別PRで実施。移行手順は [theme-contract-spec.md](../../../docs/theme-contract-spec.md) §10 と併せて計画する）。

**採る構造: バクラクの3層テーマ契約**（`[MB]`。国内で唯一、この設計思想を実装している実例）。

```
層1 骨格トークン    ニュートラル / タイポ / 余白リズム / 形状 / モーション
                    → 全ブランド共通。ブランドによって変わらない
層2 ブランドランプ  ブランドごとの色相を、全ブランドで同一の刻みを持つ段階スケールに展開
                    → 段の数と刻みは共通。色相だけが違う
層3 抽象スロット    --color-brand-surface / --color-brand-ink / --color-cta-bg …
                    → 「いま表示しているブランド」を指す役割名。コンポーネントはこれしか参照しない
```

切替は **`data-brand` 属性**で行う。`<Page brand="peerdesk">` が `data-brand="peerdesk"` を出し、
CSSがスロットの向き先を差し替える。バクラクはプロダクトLPごとに 800バイト程度のCSS 1枚を
追加読み込みする方式だったが、こちらは配布物が1枚のCSSなので属性セレクタで足りる。

**シリーズ既定 + 製品上書き**を許す。ピアデスクシリーズの既定ランプをシリーズが持ち、
配下の個別製品が必要なときだけ自分のランプへスロットを向け替える。
バクラクが `base.css` の既定をシリーズ色（bakuraku）に向けていたのと同じ形（`[MB]`）。

**契約の詳細仕様は別文書 [theme-contract-spec.md](../../../docs/theme-contract-spec.md) に委譲する。**
スロット名の一覧、ランプの段数と刻み、`data-brand` の適用範囲、
ダークモードとの掛け合わせ、codegen の入出力形式はそちらで確定させる。
本文書では「3層である」「切替は `data-brand`」「シリーズ既定+製品上書き」の3点のみを決める。

#### 注意: 色相を振ることは主流ではない

`[MB]` の実測では、**6社中4社（freee / マネーフォワード / SmartHR / Sansanの一部）は
プロダクト間でアクセントを変えていない。** 色相を振っているのはバクラクとラクスの2社だけで、
**どちらも10製品以上を1シリーズに束ねているケース**である。

したがって「3ブランドだから色相を3つ」という導出は他社実態からは出てこない。
それでも契約を先に作るのは、**ピアデスクシリーズの配下に製品が増えることが確定しているから**であり、
契約が無い状態で製品が増えると生hex直書きが始まるから。
**契約は作るが、色相を実際に何本振るかはブランド判断であって、この文書では決めない。**

#### 破れ方への備え

バクラクは契約を持っていてもなお破れている（`[MB]`）。

| 破れ方                                 | 実例                                                                               | 対策                                                     |
| -------------------------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------- |
| トークンを経由せず生hex直書き          | `clm.css` が `#A35EA1` 等を直書き                                                  | stylelint で hex リテラルを禁止（Stage 2）               |
| 他ブランドのランプを参照するコピペ残り | `attendance.css` / `ar-management.css` に `--color-product-10: var(--docissue-10)` | ランプ名とブランド名の一致をテストで検証                 |
| ランプ位置の混在                       | primary が `-60` と `-70` に分裂し L\* が 38.5〜57.0 にばらける                    | 「操作色はランプの固定段から採る」をテストで縛る（次項） |

### 1-2. 操作色と装飾色 — 同一ランプの2つの段

現行の `primary`（操作用）/ `brand`（装飾用）の2階層は、**ブランドを増やしても成立するように
「別々の色」ではなく「同一ランプの2つの段」として一般化する。**

| 役割                                | ランプの段               | 保証                         | 禁止                                       |
| ----------------------------------- | ------------------------ | ---------------------------- | ------------------------------------------ |
| **操作色** `--color-brand-action-*` | OKLCH **L ≈ 0.553** の段 | 白文字 4.5:1 以上（WCAG AA） | —                                          |
| **装飾色** `--color-brand-decor-*`  | OKLCH **L ≈ 0.73** の段  | —                            | **明背景のテキスト・ボタン背景に使用禁止** |

現行の値はすでにこの構造になっている（計算値）:

| 現行トークン  | hex       | OKLCH L    | 白背景コントラスト |
| ------------- | --------- | ---------- | ------------------ |
| `primary-500` | `#008575` | **0.5527** | 4.55:1             |
| `brand-500`   | `#13c3a0` | **0.7299** | 2.25:1             |

つまり**新しい制約を導入するのではなく、いま現行値が満たしている L の関係に名前を付けて他ブランドへ展開する。**

ただし厳密には、現行2色は同一色相ではない（`primary` H≈180.4 / `brand` H≈173.4 と **7°ずれている**。
ルート CLAUDE.md の「両方 H≈173」は不正確）。契約の不変量は**各段の L / C** であって
「ランプ内の色相が単一であること」ではない。単一色相から生成するのは新ブランド用の既定パスであり、
**コーポレート（ティール）は現行 hex を一切変更せず explicit 登録とする。**

段を揃えるだけでは不十分である点も明記する。
バクラクは `-60` を採る7プロダクトの L\* を 49.5〜57.0（幅7.5）に収めることに成功しているが、
**その7色の白背景コントラストは 3.50:1〜4.57:1 で、4色が WCAG AA 未満**（`[MB]`）。
**ランプ段の統一と AA 適合は別の話**であり、操作段は色相ごとに実測してAAを確認する。
`packages/tokens/src/tokens.test.ts` がすでにコントラスト比を実計算しているので、
新ブランドのランプを足すときは同時に期待値を追加する。

### 1-3. 第3の役割 — CTA専用アクセント（オプション）

`--color-cta-bg` / `--color-cta-ink` を**オプションのスロット**として用意する。
未指定なら操作色にフォールバックする。

根拠はカミナシの実測（`[LP]`）。ブランドの主色は `#0360e5`（青）だが、
固定ヘッダー内のプライマリCTAの塗りは **`#ffdb22`（黄）でブランド色と別**。
freee の下部固定バー部品も `#2864f0` とブランド色を使っており、社によって判断が分かれる。

**「CTAはブランド色である」を構造に焼き込まない。**
振り分けの判断はブランド側に残し、システムはスロットだけ持つ。

### 1-4. 色のガバナンス

**デザインシステムが持つのは、ルール・生成器・検証・レジストリの4つだけ。**
**色相の値そのものはブランドの判断であり、この文書でも `packages/tokens` でも先に決めない。**

| DSが持つもの | 内容                                                                                                              |
| ------------ | ----------------------------------------------------------------------------------------------------------------- |
| ルール       | ランプの段数と刻み。操作段 L≈0.553 / 装飾段 L≈0.73。ニュートラルは共通で固定                                      |
| 生成器       | 色相を1つ与えるとランプ全段を生成する関数（OKLCH の L と C を固定して H だけ回す）                                |
| 検証         | 操作段の白文字コントラスト、装飾段の誤用、ランプ位置の統一、hexリテラルの混入                                     |
| レジストリ   | ブランド名 → 色相 の対応表を1箇所に持つ。ラクスがコーポレートCSS1枚に10製品色を登録していたのと同じ発想（`[MB]`） |

ニュートラルを共通で固定することは実態に裏付けがある。
**色を振っている2社（ラクス10製品色 / バクラク13プロダクト色）は、どちらもニュートラルを
全サイトで完全共有している**（`[MB]`）。「アクセントを振るなら、ニュートラルは絶対に固定する」。

### 1-5. 和文タイポグラフィ

`[VL]` の8社実測とデジタル庁DADSから、**候補レンジではなく確定値**として置く。

| 項目                | 確定値                                   | 根拠                                                                                                                                  |
| ------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 見出しの行間        | **1.3〜1.4**                             | 値が取れた6社すべてが主要見出しでこの帯。デジタル庁 Display の 140% とも一致（`[VL]`）                                                |
| 本文の行間          | **1.6〜1.75**                            | デジタル庁「一般的な本文」160%〜「心理的負荷の軽減」175% と実サイト中央値が重なる帯。LayerX の 1.9 は最外れ値として採らない（`[VL]`） |
| 負の字送り          | **禁止**                                 | 8社中**0社**。和文の詰めは `palt` で行い `letter-spacing` はアキ方向にしか使わないのが共通の作法（`[VL]`）                            |
| 見出しの字送り      | **0 〜 +.05em**                          | Sansan +.03 / LayerX +.04 / バクラク +.05 / ナレッジワーク +.03〜.08（`[VL]`）                                                        |
| ウェイト補正        | **Regular +.04em / Bold +.03em**         | ナレッジワークが全段で貫いている唯一の体系的先行例。太字は字面が膨らむぶんアキが広く見えるのを打ち消す（`[VL]`）                      |
| `palt`              | **見出しとラベルのみ。本文には付けない** | ナレッジワーク方式。LayerX の全面適用と2択で、本文に効かせないのは意図的な判断（`[VL]`）                                              |
| `line-break`        | **`strict`**                             | SmartHR のみの先行例。禁則を厳格にする（`[VL]`）                                                                                      |
| `text-spacing-trim` | **`space-all`**                          | SmartHR のみの先行例。`palt` に依存せず約物アキを詰める（`[VL]`）                                                                     |
| 本文の下限          | **14px未満を禁止**                       | デジタル庁が「原則として許容されない」と明記。14px 自体も付随情報に限る（`[VL]`）                                                     |
| `text-wrap`         | **`balance` を見出しに採用**             | **国内BtoB 8社中0社。先行例ゼロの領域**（`[VL]`）                                                                                     |

#### `text-wrap: balance` を先行例ゼロでも採る理由

`[VL]` の実測では、和文の改行制御は `text-wrap` ではなく
**`<br>` の手動挿入・画像化・`white-space: nowrap`** で行われている。
バクラク・SmartHR・カミナシのヒーロー見出しはいずれも `<br>` か画像で改行位置を固定している。

**手動の `<br>` は、コピーを人が書く前提の解法である。**
本システムはAIが日本語コピーを量産する前提であり、AIに `<br>` の適切な位置を判断させると
必ず暴れる（「見出しが3行に折り返してヒーローが崩れる」は既知の最頻事故）。
`text-wrap: balance` は**この手動作業の自動化**として採る。

先行例が無いことのリスクは、Stage 5 の和文最悪ケース VRT で吸収する。
`balance` が効かないブラウザではただ折り返すだけなので、劣化は緩やかである。

### 1-6. 既存バグの修正

`packages/ui-web/src/styles/theme.css` に2件。

**(a) `:lang(ja)` の `font-feature-settings` が body の指定を丸ごと上書きしている**

```css
body {
  font-feature-settings: 'cv01', 'cv03', 'cv04', 'tnum';
}
:lang(ja) h1,
:lang(ja) h2,
:lang(ja) h3 {
  font-feature-settings: 'palt';
}
```

`font-feature-settings` は加算されない。和文ページの h1〜h3 では `tnum`（等幅数字）が失われ、
**数値訴求の桁がガタつく。** 数値訴求は 19/19 ページが持つ必須要素（`[LP]`）であり、
見出しに数字が入るケースは頻出する。全機能を明示的に並べて書く。

**(b) `.lang-ja` / `.lang-en` がデッドコード**

どちらのクラスもコンポーネント側で一度も付与されていない（使用0件）。
和文の行間はこの2クラスではなく、1-5 のタイポスケールが担う。削除する。

### 1-7. radii / shadow / motion

トークンの共通化の境界線を引き直す。現在 `@siracusahq/tokens` にあるもののうち、
**ブランドとして本当に共通なのはニュートラル・フォントファミリ・余白リズムだけ**。

```
tokens（共通のまま）     neutral scale / fontFamily / fontWeight / spacing 4pxグリッド
ui-app 固有             radii(小) / shadow(UI) / motion(100–300ms)
ui-web 固有（新設）      radii(役割名) / shadow(2層) / motion(二系統)
                        和文タイポスケール
```

- **`radii` は役割名にする。** 現状 `rounded-full` 15回 / `rounded-2xl` 9回とスケール上端に
  張り付いているのは、px値で選ばせているから。`--radius-card` / `--radius-pill` /
  `--radius-media` / `--radius-section` のように、**どこに使うかで名前を付ける**。
  バクラクが `--radius_section: 20px` / `--radius_section-inner-md: 10px` という
  役割名トークンを持っているのと同じ形（`[MB]`）
- **`shadow` は2層にする。** UI操作用の小さい影と、LPの「浮いたカード」用の
  大きく柔らかい影は別物。1つのスケールに混ぜない
- **`motion` は二系統にする。** UI応答（100〜300ms、`ease-out`）と
  演出（400〜900ms、expo系）を分ける。`AnimateOnScroll` のハードコード
  （600ms / ease-out）は演出系トークンへ戻す

**注意**: 二重管理の検出（`tokens.test.ts`）は無効化しない。正本の置き場所が
変わるだけで、CSS変数との突き合わせは維持する。Stage 2 でトークンを codegen 化すれば、
このテストが守っている事故そのものが構造的に消える。

---

## Stage 2: 全コンポーネント1周

### 2-0. 進め方 — 縦切り

35コンポーネントを横方向に「全部 props 削除 → 全部 CSS Modules → 全部 画像対応」と
進めると、**最初の1ページが動くのが最後**になる。縦に切る。

1. **プリミティブ**（`Container` / `Section` / `Heading` / `Text` / `Eyebrow` /
   `MarketingButton` / `Grid` / `MediaFrame`）を新しい形で完成させる
2. **実ページに必要な5〜6セクション**だけを1周させる
   （`HeroSection` / `LogoCloud` / `FeatureGrid` / `StatsSection` / `CTASection` / `Form`）
3. **その5〜6セクションで実ページを1本組んで検証する。**
   ここで契約の穴が出る。残り30本を書き終えてから出るより、圧倒的に安い
4. 残りのセクションを同じ形に揃える

各コンポーネントは**1回だけ触る**。props削除・CSS Modules化・ビジュアル資産対応を同時に行う。

### 2-A. props の棚卸し

#### 全セクション共通で削除する

| prop           | 選択肢 | 使用回数 | 判断                                 |
| -------------- | ------ | -------- | ------------------------------------ |
| `background`   | 4      | 35       | **削除** — ページが決める（Stage 3） |
| `spacing`      | 5      | 7        | **削除** — ページが決める（Stage 3） |
| `eyebrowStyle` | 6      | 0        | **削除** — トーンが決める（Stage 3） |

`background` が35回使われているのは「必要だから」ではなく、
**セクションが決めるべきでないことを毎回決めさせられている**証拠。

`eyebrowStyle` の削除に伴い、`Text` の `size` にある overline 系7バリアント
（`overline` / `overline-pill` / `overline-border` / `overline-text` / `overline-dot` /
`overline-gradient` / `overline-icon-pill`）も整理し、独立した `Eyebrow`
コンポーネントに切り出してトーン連動にする。

#### 個別に削除する

| コンポーネント                                            | prop                       | 理由                                                                                                              |
| --------------------------------------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `HeroSection`                                             | `titleGradient`            | 未使用。かつ**実装が破綻している**（`title` を素で1回、`GradientText` でもう1回、`<br/>` を挟んで二重に描画する） |
| `HeroSection`                                             | `backgroundPattern`（5）   | トーンが決める                                                                                                    |
| `HeroSection`                                             | `layout`（3）              | `image` の有無 + `imagePlacement: 'side' \| 'below'` に統合                                                       |
| `CTASection`                                              | `backgroundMesh`           | トーンが決める                                                                                                    |
| `CTASection`                                              | `logoStrip`                | 未使用。`LogoCloud` と役割が重複                                                                                  |
| `FeatureGrid`                                             | `cardStyle`                | 未使用。見た目のみの差                                                                                            |
| `CodeBlock`                                               | `alignment`                | 見た目のみの差                                                                                                    |
| `CodeBlock`                                               | `layout`                   | `description` の有無から導出できる                                                                                |
| `StatsSection`                                            | `animated`                 | 常時ON + `prefers-reduced-motion` を尊重                                                                          |
| `LogoCloud`                                               | `scrolling`                | ロゴ件数から導出（8件以上でスクロール）                                                                           |
| `FeatureGrid` / `CaseStudySection` / `TestimonialSection` | `columns`                  | 件数から導出                                                                                                      |
| `BentoGrid`                                               | `BentoItem.variant`（3択） | アイテム単位の見た目指定。AIが1件ずつ指定するとまだら模様になる。**位置で自動決定する**（1件目のみ強調）          |
| `HeroSection` / `CTASection` / `PricingCard`              | `actions[].variant`        | 1つ目を primary、以降を secondary に自動割当                                                                      |

`columns` の自動導出ルール:

| 件数 | 列数     |
| ---- | -------- |
| 2    | 2        |
| 3    | 3        |
| 4    | 2（2×2） |
| 5–6  | 3        |
| 7+   | 3        |

これにより「4件の機能を4列に潰して1件あたりが細くなる」事故が構造的に消える。

#### 残すもの

`eyebrow` / `title` / `subtitle` / 各セクションのデータ配列（`features` / `plans` /
`testimonials` など）/ `CodeBlock` の `language`・`filename`・`showLineNumbers`・
`highlightLines` / `CTASection` の `note`・`socialProof` / `HeroSection` の
`badge`・`actions`・`image`。

判断基準は **「意味が違うか、見た目が違うだけか」**。後者は消す。

#### 削減の効果

10セクションのLPで表現しうる見た目の組み合わせ:

```
現状:  20^10 ≈ 10兆通り（うち成立するのは十数通り）
再設計後: ブランド × トーン3通り × コンテンツ
```

### 2-B. ビジュアル資産

#### 現状

画像に関する実装は**事実上ゼロ**。

| 観測                   | 値                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------ |
| 実装コード中の `<img>` | **0件**（テストコード内に1件のみ）                                                         |
| `aspect-*` の使用      | **0件**                                                                                    |
| 画像を受ける口         | `HeroSection.image` / `FeatureShowcase.image` の2つのみ。いずれも `React.ReactNode` 素通し |

`ReactNode` 素通しは「何を渡してもよい」ということであり、**AIは毎回違うものを渡す。**
`<img>` を裸で渡す、`<div>` に background-image を書く、SVGを直接埋める。
高さが確定しないのでレイアウトシフトが起き、影も角丸も枠もページ内でバラバラになる。

#### 追加する部品

| 部品               | 責務                                                                                                                                                   | 実測の裏付け（`[VL]`）                                                                                                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MediaFrame`       | **固定アスペクト比**の枠。読み込み前から高さを確保する。角丸・影・背景はトークンから。中身は `img` / `video` / `svg` を問わない                        | 縦横比の固定はレイアウトシフト対策であり実測由来ではない。ただし枠・影・角丸の値をコンポーネント側で固定する必要は、8社が処理手法を統一していることから導ける                                              |
| `ProductShot`      | プロダクト画面専用。**正対のみ。傾き・パースは実装しない**。デバイスモックの有無を選べる。ブラウザ枠は付けない                                         | **8社中「傾き・パース付きスクリーンショット」は0社。** カミナシ・freee とも正対。カミナシはデバイスベゼルあり・ブラウザ枠なし・影控えめ                                                                    |
| `LogoMark`         | 導入企業ロゴ。**光学サイズ正規化**（縦横比の違うロゴを、面積が揃って見えるようにボックス内で正規化する）。グレースケール化の可否をブランド側で切り替え | ロゴ帯は 13/19 ページが保有し、掲載時の中央値は約20社（`[LP]`）。20枚のロゴを素の `<img>` で並べると横長ロゴだけが巨大に見える。グレースケール化の実態は**判定不能**（`[LP]`）のため既定は決めず切替にする |
| `Avatar`           | 人物写真。円形マスク・固定サイズ。テスティモニアル用                                                                                                   | freee のKVで人物写真が円形マスクの小さなアクセントとして使われている                                                                                                                                       |
| プレースホルダ機構 | 画像未指定時に、崩れず・目立ちすぎず・**「ここに画像が要る」と分かる**面を出す                                                                         | AI生成LPは画像が後追いになる。空 `ReactNode` で高さ0に潰れるのが現状の挙動                                                                                                                                 |

#### 規則1: 抽象（図・UI断片）は必ず具象物とペアにする

`[VL]` の実測。**抽象だけで押し切っている社は0社。**

- バクラク: 半透明のUI断片コラージュに、**写実的な紙の帳票・領収書・電卓**を組み合わせ、
  「紙 → デジタル」の対比を作っている
- freee: 申告フォームのスクリーンショットに、**3D風の風船・雲・紙吹雪・マスコット**を添えている

図解やコードだけを置くと、何のサービスかが伝わらない。ペアの相手を必ず持つ。

#### 規則2: 主役ビジュアルは1ブランド1種類

`[VL]` の実測で、1種類に絞っている社（ナレッジワーク=タイポのみ / HQ=アイソメのみ /
カミナシ=デバイスモックのみ / Sansan=線画イラストのみ）と、
混在している社（freee=スクショ+3D装飾+抽象モチーフ+写真の4種 / SmartHR=実写+レタリング画像+図解画像）
がはっきり分かれる。

**混在させるなら、少なくとも1系統には数値レベルの規律を持たせる。**
freee は4種混在だが、**モチーフSVGだけは viewBox 180×180 統一・2色・幾何形状のみ**
という規律を敷いて破綻を防いでいる（`[VL]`）。

#### 規則3: ブランド別の視覚デバイス

| ブランド               | 主役デバイス                                             | 根拠                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Polastack**          | **アーキテクチャ図・コードを主役に置く**                 | `[VL]` の8社に「図解・コードをヒーローの主役に据えた社」は**0社**。ただし**8社すべてが非エンジニア向けの水平SaaS**（経理・人事・労務・営業・現場）であり、買い手が技術者ではない。**この空白は「効かない」の証拠ではなく「買い手が違う」の証拠**である。技術者向けでは図解・コードをヒーローに置くのは常道であり、Polastack の買い手は技術者。**なお調査対象に開発者向けインフラ／API系企業は1社も含まれておらず、この領域は未調査**（`[VL]`） |
| **ピアデスクシリーズ** | **水平SaaSの作法に従う**（人物・具象物・UIモックの正対） | 買い手が非技術者。`[VL]` の8社と同じ市場。図解はヒーローではなく**必ずヒーローより下**に置く（SmartHR の機能連携図、freee のモチーフSVG、いずれもヒーロー下）                                                                                                                                                                                                                                                                                  |
| **コーポレート**       | タイポグラフィ主役                                       | corporate-top はコンバージョンCTAを持たない型（`[LP]` LayerX）。訴求対象が投資家・採用候補であり、製品ビジュアルは要らない                                                                                                                                                                                                                                                                                                                     |

図解を主役にする場合のコストも先に織り込む。
SmartHR は図解を画像化し、**`alt` に図解内容を数百字の全文テキストで書く**ことで
アクセシビリティを担保している（`[VL]`）。`MediaFrame` は `alt` を必須にする。

### 2-C. CSS Modules への移行（案C）

#### なぜ Tailwind の配布をやめるか

利用者は Astro を前提としており、Tailwind を持っているとは限らない。
また Tailwind クラスを配布したままだと以下が起きる。

- **`@theme` 変数がグローバルを汚す**。`--color-primary-*`・`--font-sans`・`--radius-*`
  を宣言するため、利用者が自分の Tailwind テーマを持っていると読み込み順で
  一方が勝つ。preflight は利用者のページ全体をリセットする
- **`className` が静かに効かない**。AIは高確率で `<HeroSection className="mt-24">` と
  書く。利用者に Tailwind がなければ何も起きず、あってバージョンが違えば微妙にずれる。
  どちらもエラーにならず Storybook でも再現しない
- prefix による名前空間分離は**設定1行では済まない**。`prefix(ps)` を付けると
  利用者が書くクラス名が `ps:flex` になるため、全コンポーネントのクラス文字列を
  書き換える必要がある（実測: prefix 適用時の生成ユーティリティは0件）

#### なぜ vanilla-extract ではなく CSS Modules か

どちらを選んでも**利用者には見えない**（コンパイル済みCSSを配るため）。
したがってこれは純粋に自社内の実装体験の選択であり、以下2点で CSS Modules を採る。

1. **AIの実装精度**。35コンポーネントを書き、その後も保守するのは主にAI。
   素のCSSでの精度は明確に高い。vanilla-extract は `.css.ts` がビルド時実行である
   ことを理解できず、runtime値を参照して壊す失敗を繰り返す
2. **既存のビルド構成を壊さない**。esbuild 0.25 は CSS Modules をネイティブに処理する
   ことを検証済み。tsup 設定を変更せずに動き、コンポーネント単位エントリのおかげで
   **CSSも自動的にコンポーネント単位で分割される**

移行方向の非対称性も理由になる。CSS Modules → vanilla-extract は容易
（トークンはCSS変数として存在し、cva → recipe は構造が同じ）だが、逆は型情報を捨てる作業。

#### 必須の3点セット

CSS Modules は既定では「静かに壊れる」。CSSの `.gradient` をリネームすると
`s.gradient` が `undefined` になり `class="base undefined"` が出力される。
以下を必ず併せて導入する。

1. **トークン → CSS変数の生成**。`@siracusahq/tokens` から `theme.css` を codegen し、
   値の二重管理を構造的に消す。**ブランドランプの生成もここに乗せる**
   （色相を1つ与えるとランプ全段が出る。1-4 の「生成器」の実体）
2. **`*.module.css.d.ts` の生成**。typecheck に含め、クラス名のリネーム・typo を
   型エラーにする。**これを省略するなら CSS Modules は vanilla-extract より劣る**
3. **stylelint で `var()` を既知トークンに制限**。存在しない変数参照を落とす。
   **併せて hex リテラルの直書きを禁止する**（バクラクの `clm.css` と同じ破れ方を止める）

#### 配布物

- コンパイル済みCSS（Tailwind は不要）
- Web フォントを `@font-face` として同梱（現在の未配信問題を同時に解決）
- `pnpm size` に**CSSサイズの枠を追加**する。現状JSしか測っておらず、
  CSSが肥大しても誰も気づかない

### 2-D. コンポーネントの追加・拡張

`[LP]` の実測で、現行のコンポーネント群に穴があることが分かった。

#### `StatsSection` → 数値訴求の4スロット

**19/19 ページが何らかの数値訴求を持つ。例外なし**（`[LP]`）。
型は4つに整理できる。

| スロット         | 出現  | 実例                                                         |
| ---------------- | ----- | ------------------------------------------------------------ |
| ① 導入規模       | 15/19 | 20,000社 / 62万事業所 / 80,000社 / 17,000現場                |
| ② No.1・シェア   | 11/19 | 労務クラウド7年連続シェアNo.1 / クラウド会計シェアNo.1 56.3% |
| ③ 継続率・満足度 | 6/19  | サービス継続率99%以上 / サポート満足度97%                    |
| ④ 削減率・改善率 | 10/19 | 作業量75%削減 / 工数88%削減 / ミス98.3%削減                  |

①と②の同時掲出が最頻（10/19）。「規模」+「順位」のペアが国内BtoBの定型。

**`asOf`（時点注記）を必須にする。** 「20,000社（2026年6月現在）」「62万（2025年3月末時点）」
のように、ほぼ全社が時点を併記している（`[LP]`）。これは意匠ではなく**景品表示法対応**であり、
時点のない No.1 表示は不当表示になりうる。**型で必須にして、書き忘れをビルドで止める。**

#### `SecurityBadges` → 3系統に分ける

17/19 ページが何らかの信頼性バッジを掲出している（`[LP]`）。現行の実装は1系統しかない。

| 系統                   | 内容                                                                         | 実例                                                                                                         |
| ---------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| ① 認証                 | ISMS / ISO27001 / 27017 / SOC2 / Pマーク / JIIMA / TRUSTe                    | 10ページがバッジ画像を掲出                                                                                   |
| ② 第三者レビュー・受賞 | ITreview Best Software in Japan / HDI格付けベンチマーク / Manegy管理部門大賞 | kintone・カミナシ・HRBrain。**国内BtoB固有の信頼性デバイスで、当初の設計仮説に無かった**                     |
| ③ 法定表示             | 業法の許可番号・所属先の明示                                                 | Bill One が銀行代理業の許可番号（関東財務局長（銀代）第480号）と所属銀行を掲出。金融・人材・不動産系では必須 |

#### `CTASection` / `MarketingButton` → `kicker` スロット

CTAの上に載る「＼／囲みの上乗せ文言」。実測で7種確認（`[LP]`）:
「＼5分でわかる資料をプレゼント／」「＼詳しい料金がわかる／」「＼カミナシがもっとわかる3点セット／」
「＼同業他社の事例を紹介中！／」「＼5日の業務がたった1日で終わる！／」「クレジットカード不要」
「ご相談だけでもOK」。

**重いCTAに「重さを下げる修飾」を付ける慣行**（HRBrain の「ご相談だけでもOK お問い合わせ」）が
あり、これはボタンラベルとは別のスロットとして持つ必要がある。

#### `ContentHub` を新設

**製品系12ページ中9ページが、末尾に資料・セミナー・コラム・News の回遊セクションを持つ**（`[LP]`）。
国内BtoBの強い特徴で、LPの末尾がメディアハブ化している。現行のシステムにこの部品は無い。

#### 2段階CTAは「2オファー」として設計する

現行の想定は「軽（資料DL）+ 重（問い合わせ）」の軽重2段階だったが、
実測では**「軽 + 軽」が4件ある**（`[LP]`）。

| FVの構成                  | 件数                                  |
| ------------------------- | ------------------------------------- |
| 軽 + 重                   | 7                                     |
| **軽 + 軽**（資料DL 2本） | **4**（バクラク3ページ全部 + ANDPAD） |
| 軽1本のみ                 | 1（カミナシ。重は追従要素へ）         |
| 重のみ                    | 1（HRBrain）                          |

バクラクは「＼5分でわかる資料をプレゼント／ メールで資料を受け取る」+
「＼詳しい料金がわかる／ 料金表をもらう」という**2本とも軽いCTA**を並べている。
これは軽重の2段階ではなく、**「何の資料が欲しいか」の分岐**であり質的に別の設計。

またセルフサーブ型（freee / MF / chatwork）では重いCTAが「問い合わせ」ではなく
「無料で始める」になり、重さの性質が営業接触ではなくアカウント作成に変わる。

→ **`primary` / `secondary` に軽重を焼き込まない。** `offers: [Offer, Offer]` として、
各オファーが自分のラベル・kicker・遷移先・計測IDを持つ形にする。

#### `FormSection` に `onSubmit` の口を開ける

現行は **Formspree 固定**（`formspreeId` prop を受けて `https://formspree.io/f/{id}` に
直接 fetch する）。npm パッケージとして配る以上、利用者の送信先を決め打ちしてはならない。

`onSubmit(values) => Promise<void>` を第一級にし、Formspree は
「`formspreeId` を渡したときに使われる既定の実装」に降格する。

実測では **15/19 ページがフォームを別ページに置いている**（`[LP]`）ため、
埋め込みフォームは lead-gen 型の主役であって、product 型の必須部品ではない。

#### 全CTA・フォームに計測フック

`data-cta` 属性と `onCTAClick` コールバックを、CTAを出しうる全コンポーネントに通す。
LPは成果を測れなければ改善できないが、**現状はクリックを捕捉する口が1つも無い。**
Stage 4 でページ層から一括で受け取れるようにする。

---

## Stage 3: ページ層

AIの品質事故はコンポーネント単体ではなく**構成**で起きる。
セクションの順序、CTAの数、証拠の出し方。ならば構成そのものを配る。

### 3-1. ページ型5種

`[LP]` の19ページ実測で、当初の4種から組み替えた。

| 型                                  | 内容                                                                                                                                                                              | 実測                                                                                                                   |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `lead-gen`                          | 資料DL・問い合わせの獲得専用。**グローバルナビを剥がす**（2/2 でそうしている）。資料の中身の箇条書き + 数値訴求 + フォーム + 最小フッター                                         | freee whitepaper、カミナシ inquiry                                                                                     |
| `product`                           | 単一製品のLP。最多                                                                                                                                                                | バクラク経費精算、freee会計、freee人事労務、MF、Bill One invoice、chatwork                                             |
| **`product-portfolio-top`（新設）** | **複数プロダクトの玄関。カードグリッドで下層LPへ分岐させるセクションが中核。** 末尾がメディアハブ化する                                                                           | **19ページ中7ページ**（バクラクトップ / SmartHRトップ / Bill Oneトップ / カミナシトップ / ANDPAD / HRBrain / kintone） |
| `case-study-list`                   | 事例の**一覧型**。キャッチコピー型のヒーローではなく短いページタイトル。ピックアップ + **多軸フィルタ**（サービス / 業種 / 従業員規模 / 課題）+ カードグリッド + ページネーション | SmartHR `/case/`、バクラク `/case/` の2/2 がこの形                                                                     |
| `corporate-top`                     | ビジョンステートメント型。**コンバージョンCTAを持たない。** 導入ロゴ0社、数値は事業規模・投資家向け。採用が主要導線                                                               | LayerX                                                                                                                 |

**`product-portfolio-top` が最重要。** これは「コンパウンド企業の主戦場」であり、
Polastack もピアデスクシリーズも、まずこの型のページを必要とする。
`corporate-top` でも単一 `product` でもない独立した型であることが、実測で確認された。

**個別事例記事（`case-study-detail`）は今回作らない。** `[LP]` で1本も取得できておらず、
構成のデータが無い。推測で型を作ると、根拠のない構成を配ることになる。

> **【2026-08-13 追記】保留を解除し、`case-study-detail` を6つ目のページ型として追加した。**
>
> 保留の理由は「データが無い」という**手法の限界**であって、作らないという設計判断ではなかった。
> `research-eyebrow.md` §1-1 で実ブラウザ計測（Playwright + `getComputedStyle`）が確立し、
> その制約が外れたため、**9 サイト × 3 記事 = 27 記事**を再計測した
> （[docs/research/research-case-study-detail.md](../../../docs/research/research-case-study-detail.md)）。
>
> - 事例記事は**サイト内で構成・書式が固定**されており（7項目の一致率 **97.6%**）、
>   記事ごとに変わるのは章数・文字数・写真枚数という「量」だけだった。
>   **パターンとして配る効果が LP のどのページ型よりも高い**（記事は1サイトに数十〜数百本ある）
> - **作らないもの**も実測で決まった: 冒頭の数値タイル **0/27**・引用の飾り枠（`blockquote`）**0/27**・
>   目次 2/9・章ごとの面の交替 0/9（本文は 9/9 が単一面）
> - メタ情報の軸（業種 / 従業員規模 / サービス / 課題）は 9/9 サイトで一致し、
>   `case-study-list` と**同じ型（`CaseStudyMeta`）を共有**できる
>
> これに伴い、末尾の未決着リスト #5（「`case-study-detail` の構成 — 1本も取得できていない」）は閉じた。

> **【2026-08-14 追記】News / ブログの一覧・詳細を 7・8 つ目のページ型として追加する。**
>
> LP 末尾のコンテンツ回遊（`[LP]` 9/12）が指す遷移先のうち、
> 事例記事以外（お知らせ・コラム）を受け取るページ型が存在しなかった。
> 国内BtoB SaaS 9社から**一覧16ページ・記事27本**を実測した
> （[docs/research/research-news-blog.md](../../../docs/research/research-news-blog.md)）。
>
> | 型               | 内容                                                                                                     | 実測 `[NB]`                  |
> | ---------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------- |
> | `article-list`   | お知らせ / ブログの**一覧型**。キャッチコピー型ヒーローを持たない。`kind` で News / ブログを切り替える  | News 一覧 7 / ブログ一覧 7   |
> | `article-detail` | お知らせ / ブログの**記事型**。`kind` で許容フィールドが変わる判別ユニオン                              | News 記事 12 / ブログ記事 15 |
>
> - **News とブログは同じ会社の中でも別テンプレートである。** 著者は News **0/12** / ブログ **14/15**、
>   目次は News **0/12** / ブログ **12/15**、更新日は News **0/12** / ブログ 5/15。
>   一覧も `h1` が News は短いページタイトル型 **7/7**、ブログはブランド名 + タグライン型 **5/7**。
>   **両方 optional の 1 型にすると、実測に存在しない「著者と目次を持つ News」が型で許される**ため、
>   `kind` による判別ユニオンで分ける
> - **年別アーカイブは News 6/7 / ブログ 0/7**、**ピックアップ枠は News 2/7 / ブログ 6/7** と
>   一覧のフィルタ軸も逆を向いている
> - **事例記事の中核（会社プロフィール・課題/決め手/効果サマリー・話者）は News / ブログに 0/27。**
>   逆にブログの中核（著者・目次・更新日・SNS シェア）は事例記事に 0/27。
>   **`case-study-*` の拡張では済まない**ことが実測で確定した
> - **作らないもの**も再確認された: 冒頭の数値タイルは
>   事例 0/27 + News 0/12 + ブログ 0/15 = **0/54**、無限スクロール **0/14 ページ**、
>   並べ替え UI 1/14、記事内サイドバー（ブログ 3/7・News 0/7）
> - サイト内一貫性は **95.0%**（事例記事の 97.6% とほぼ同水準）。
>   揺れたのは 3 箇所すべてが「解説記事 / インタビュー / 連載」という**記事の種類の差**であり、
>   同一テンプレート内の揺れではない
> - `tone` は両型とも **`trust`**。`case-study-*` を `product` にしたのは「記事は製品面の続き」
>   だからだが、News はコーポレート発表（**コーポレートドメインに置くのが 4/7**）、
>   ブログは知識提供メディアで、どちらも製品LPの続きではない。
>   **これは実測ではなく判断である**（`research-news-blog.md` §6-1）
> - **監修者（`ArticleSupervisor`）は実測 1/7 サイトだが型に含める。**
>   税務・労務・会計といった規制領域で有資格者による監修の明示が信頼性の要件になるため、
>   実測の少数性を理解したうえでの経営判断として採用する
>
> **`[NB]` は CSS の実値を含まない**（冒頭「実測の出典表記」参照）。
> 書式は `case-study-detail` の実装値を流用する前提で進めるが、
> **ブログの読み幅だけは実装前に再計測すること**（末尾の未決着リスト #7）。
>
> 実装の分解は [article-pages-workorder.md](./article-pages-workorder.md)。

> **【2026-08-14 追記②】資料ライブラリとセミナーを 9・10・11 つ目のページ型として追加する。**
> **資料の個票は新設しない（既存 `lead-gen` で足りる）。**
>
> `[LP]` 9/12 のコンテンツ回遊、および FV の「重いオファー」（§4-3）が指す先のうち、
> 資料ダウンロードとセミナー申込を受け取るページ型が存在しなかった。
> 国内BtoB SaaS 8社から**資料一覧 7・資料個票 6・セミナー一覧 8・セミナー詳細 21** を実測した
> （[docs/research/research-resources-seminar.md](../../../docs/research/research-resources-seminar.md)）。
>
> | 型                  | 内容                                                                                          | 実測 `[RS]`               |
> | ------------------- | --------------------------------------------------------------------------------------------- | ------------------------- |
> | `resources-library` | 資料DLの**一覧型**。表紙グリッド + タイプ / 業界フィルタ。**日付を持たない**                  | 一覧 7                    |
> | `seminar-list`      | セミナーの**一覧型**。**同じ一覧の中で開催予定とアーカイブを扱う**（別ページに分ける型は 0/8） | 一覧 8                    |
> | `seminar-detail`    | セミナーの**詳細型**。`status` で許容フィールドが変わる判別ユニオン                            | 詳細 21（予定11 / 終了9） |
>
> - **資料の個票は `lead-gen` でほぼ足りる。** 10項目中9項目が実測と合致し、
>   ずれているのは **`header` を剥がす前提**の1点だけ（実測 **6/6 がグローバルナビを持つ**）。
>   `LeadGenInput` に `header?` を任意で戻せるようにする**非破壊的な緩和**で解決する。
>   `stats`（実測 0/6）は optional のため害が無い。`closing` / `related` を持たない設計は
>   **末尾 CTA 0/6・関連資料 0/6 で実測に合致していた**
> - **「フォーム直行」型は 0/7。** 一覧のカードは **7/7 が資料詳細ページを経由する**
> - **獲得系は記事系の中核をひとつも持たない。** 章 `h2` **0/27**、公開日 **0/27**、
>   著者 **0/27**、目次 **0/27**、SNS シェア **0/27**、末尾 CTA **0/27**。
>   逆に**関連コンテンツを出さない**（資料 0/6・セミナー 1/21 ⇄ 事例 27/27・ブログ 14/15）のが
>   獲得系の性質で、**フォームの手前で他ページへ逃がさない**という意図が読める
> - **終了したセミナーは録画視聴ページに変身しない。**
>   同一URLに「受付終了 / このセミナーは終了しました」として残るのが 3/9、
>   別URLのアーカイブ面を新たに作るのが 6/9。
>   **同一URLが録画視聴に切り替わる例 0/9・資料DLに切り替わる例 0/9**
>   （SmartHR は「資料等の配布はございません」と明示）。
>   アーカイブも **9/9 がフォームを通す**（動画プレイヤーの直埋めは 0/9）
> - **開催状態は二値ではない。** 実測の語彙は **14通り**あり、
>   ANDPAD は「ライブ / アーカイブ版 / ダイジェスト版 / 常設」の **4段階**を持つ。
>   ただしページ型として区別が必要なのは「日時を持つか / 視聴期限を持つか」の2つで、
>   `status: 'upcoming' | 'closed' | 'archive'` の判別ユニオンで足りる。
>   **`sold-out`（満席・実測 0/29）と `permanent`（常設・実測 1/21）は型に含めない**
>   （末尾の未決着リスト #13・#14）
> - **型の接頭辞に `Offer*` を使わないこと。** `OfferAction` / `OfferPair` は
>   §4-3 の「2オファー」= FV の CTA を指す既存の概念であり、
>   資料・セミナーの分類や画像に同じ語を使うと **API を読む AI 利用者が取り違える**。
>   共有語彙は `ContentTaxonomy` / `ContentImage` / `ContentPagination` 系で命名する
> - **作らないもの**が再確認された: 冒頭の数値タイルは
>   事例 0/27 + News 0/12 + ブログ 0/15 + 資料 0/6 + セミナー 0/21 = **0/81**、
>   無限スクロール **0/31ページ**、キャッチコピー型ヒーロー **0/31ページ**、
>   一覧の検索ボックス 資料 0/7・セミナー 0/8、ハイブリッド開催 **0/21**、有料セミナー **0/21**
> - サイト内一貫性は **84.4%**（事例 97.6% / 記事 95.0% より低い）。
>   揺れた **7箇所すべてが「開催予定 / アーカイブ」の境目**に集中しており、
>   同じ状態のページどうしなら一致する。**セミナーは「サイト単位 × 開催状態単位」で固定されている**
> - `tone` は3型とも **`campaign`**。資料もセミナーもフォームで締める獲得面であり、
>   `lead-gen` と地続きで、一覧から個票へ遷移したときに面のトーンが変わらないほうが自然という判断。
>   **これは実測ではなく判断である**（`research-resources-seminar.md` §7-1）
>
> **`[RS]` は CSS の実値を含まない**（冒頭「実測の出典表記」参照）。
> また **「満席」の実例を1件も採取できていない**（0/29ページ）ので、
> 満席状態を型に入れるかは実ブラウザ計測が可能になった時点で判断すること。
>
> これに伴い、末尾の未決着リスト **#10**（資料ライブラリ）と **#11**（セミナー）は閉じた。
>
> 実装の分解は [acquisition-pages-workorder.md](./acquisition-pages-workorder.md)。

### 3-2. 既定セクション順序

製品系12ページの最大公約数（`[LP]`）:

```
Hero
 → 社会的証明帯（ロゴ帯 or 数値バッジ）        9/12 がヒーロー直下
 → 価値・課題提起
 → 機能／サービス
 → 料金
 → 選ばれる理由                                7/12
 → 導入事例
 → サポート／導入の流れ                        5/12
 → FAQ（任意）                                 5/12 — 必須ではない
 → コンテンツ回遊（資料・セミナー・コラム）    9/12
 → 最終CTA面                                   10/12
 → Footer（信頼性バッジ）
```

**料金 → 事例の順序に変更する。** 当初の想定は「事例 → 料金」だったが、
両方を持つページでは **料金 → 事例が4件、事例 → 料金が1件**（`[LP]`）。

**FAQ は任意にする。** 12ページ中5ページ（42%）にしかない。必須セクションではない。

#### 記事系ページの順序（`[NB]`）

上記はLP（製品系12ページ）の順序である。記事ページはこれとは別の順序を持つ。

**News 記事**（実測 12 本）:

```
パンくず（11/12）
 → 記事タイトル h1（12/12）
 → 公開日（12/12。更新日は 0/12）
 → [カテゴリ / タグ]（8/12）
 → [SNS シェア]（8/12）
 → リード文（10/12）
 → 本文の章 ×4（12/12。h3 は 3/12）
 → [会社概要・問い合わせ先の定型章]（6/12）
 → [末尾 CTA]（7/12）
 → 関連記事 3 件（10/12）
 → 一覧へ戻る（10/12）
```

**ブログ記事**（実測 15 本）:

```
パンくず（14/15）
 → 記事タイトル h1（15/15）
 → 公開日（15/15） + [更新日]（5/15）
 → カテゴリ / タグ（15/15）
 → 著者（14/15。顔写真 3/15・監修者 3/15）
 → SNS シェア（15/15）
 → リード文（13/15）
 → 目次（12/15。無いのはインタビュー / 連載型のみ）
 → 本文の章 ×6 + h3 ×12（15/15）
 → [本文中の CTA バナー]（13/15）
 → [末尾 CTA]（11/15）
 → 関連記事（14/15）
 → [一覧へ戻る]（11/15）
```

**News とブログの分岐点は「著者・目次・更新日・SNS シェア」の 4 つ。**
本文量も違う（News 中央値 約2,200字 / ブログ 約7,000字 / 事例記事 約3,400字）。

**末尾 CTA を必須にしないこと。** 事例記事は 27/27 が持っていたが、
記事系は News 7/12・ブログ 11/15 で、SmartHR ニュースのように
末尾 CTA を一切持たない運用が実在する。

#### 獲得系ページの順序（`[RS]`）

記事系がさらに別の順序を持つのと同じく、**資料・セミナーはまた別の順序を持つ。**
最大の違いは**フォームで終わること**と、**関連コンテンツを出さないこと**である。

**資料の個票**（実測 6 本。順序は 6/6 で一致。既存 `lead-gen` パターンで組む）:

```
[パンくず]（3/6）
 → h1（= 資料名）（6/6）
 → 説明文（6/6）
 → 表紙画像（6/6）
 → 「この資料でわかること」箇条書き（6/6）
 → [こんな方におすすめ]（2/6）
 → フォーム（6/6。うち埋め込み 5/6）
```

**公開日・著者・目次・SNS シェア・関連資料・末尾 CTA はすべて 0/6。**

**セミナー詳細**（実測 21 本）:

```
[パンくず]（13/21）
 → h1（セミナー名 + サブタイトル）（21/21）
 → 開催状態（20/21。LIVE / 受付中 / 受付終了 / いますぐ視聴可能 ほか14通り）
 → こんな方におすすめ（18/21）
 → セミナー概要（21/21）
 → [アジェンダ / プログラム]（11/21）
 → 開催要項（日時 19/21・開催形式 18/21・参加費 13/21・[申込締切] 4/21・[定員] 2/21）
 → 登壇者（16/21。1名が最頻 10/16・最大3名・顔写真 6/21）
 → [注意事項]（9/21）
 → 申込 / 視聴フォーム（20/21。埋め込み 11 / 別ページ 9）
```

**「開催要項」と「登壇者」の順序だけがサイト間で入れ替わる。**
それ以外は 21 本で一致する。**関連セミナーは 1/21・末尾 CTA は 0/21。**

**アーカイブ（終了後）の差分**（実測 9 本）:

```
元の開催日を残す（6/9）／ 視聴期限（4/9）／ フォーム送信後に視聴URLを送る（4/9）
動画プレイヤーの直埋め 0/9 ／ 資料DLへの切替 0/9
アジェンダは「トピックス（約45分）」のように所要時間付きに変わる
```

**関連コンテンツを型に入れないこと。** 事例記事 27/27・ブログ 14/15 が持つのに対し、
資料 0/6・セミナー 1/21。**フォームの手前で他ページへ逃がさない**のが獲得系の設計である。

### 3-3. リズム

背景と余白はセクション単体では決められない。**前後関係で決まる。**
ページコンテナがルールで割り当てる。

#### 背景 — 未検証であることを明記する

当初の「本文セクションは default ↔ muted を交互に」という規則は、
**`[LP]` では検証できていない。** ブラウザレンダリングもスクリーンショット取得も
行っていないため、実サイトの背景の明暗リズムは**一切分かっていない**。

「ヒーローと最終CTAのみ強調面」も同様に未検証で、**むしろ反証の気配がある**。
カミナシは本文中に「導入までの流れ」「セミナー」など多数の面を持ち、
freee会計・バクラクは本文中にセキュリティ専用セクションを持ち、
Bill One は「Bill One Bank」の独立ブロックを持つ。強調面が2枚に収まる構造には見えない（`[LP]` の推測）。

→ **交互リズムは「他社がそうしているから」ではなく「自分たちがそう決めたから」として実装する。**
**基準は Stage 5 のページ単位VRTで自分たちで作る。** 最初のスナップショットが基準になる。

確定で置ける規則は1つだけ。**暗い面の連続を禁止する**（3連続で dev 警告）。
これは可読性の問題であって、他社実態の問題ではない。

**追記（2026-08）**: この「未検証」は `docs/research/research-eyebrow.md` §4-3 の面の実測で
部分的に解消された。8 サイト中 5 サイトが淡い**有彩色**面（対比 1.04〜1.12:1・中央値 1.07:1）を
使っており、交替回数は 1〜3 回が主流で、機械的な ABAB ゼブラの実サイトは1件も確認できなかった。
これを受けて LP 系のページ型（`product` / `product-portfolio-top` / `lead-gen`）の面を
**ブランドティント淡色（白 50% + ramp-50）+ パターンによる明示割当**（`<Page surfaces>`）へ改訂した。
自動ゼブラは既定として残っており、事例系・記事系（`article-*`）・
獲得系（`resources-library` / `seminar-*`）は従来どおり（ティント化の要否は別途検討）。

`corporate-top` だけは**両者の中間**を採った（2026-08-16 のブランド判断）。読み手が買い手ではなく
どの節も等価に並ぶ型なので、LP のような「白の連続 + 塊で浮かす」割当は合わない。**交互リズムは
そのままに、沈んだ面の色だけをニュートラルからティントへ差し替える**（`<Page autoSurface="tinted">`）。
`about` / `stats` が任意スロットであるため、`surfaces` の添字割当では省略時にティントが
連続・消失しうる。どのスロットが沈むかを自動に任せる `autoSurface` はその破れ方をしない。

#### 余白

- 単一のリズムスケールを使う
- 関連するセクション間は詰め、話題が変わるところで開く（章立て）

#### CTA — 「2箇所まで」を撤回する

当初の規則「1ビューポート1つ・ページ全体で2箇所まで」は**明確に反証された**（`[LP]`）。

| 観測                            | 値                                                                    |
| ------------------------------- | --------------------------------------------------------------------- |
| 総CTA数の中央値                 | **15〜16本**                                                          |
| 「2本まで」に該当するページ     | **0件**                                                               |
| プライマリCTAの**ラベル種類**数 | **2種が最頻**（9ページ中8ページ）                                     |
| 反復回数                        | バクラク経費精算 = 「メールで資料を受け取る」×5 +「料金表をもらう」×5 |

→ **新しい規則: プライマリCTAの「ラベル」は2種類まで。反復は自由**（目安4〜6回）。

ページコンテナはラベルの種類数を数え、3種類目が現れたら dev 警告を出す。
**回数は数えない。** 同じ2種をセクション区切りごとに繰り返し置くのが実態の標準形。

これは実装にも影響する。`CTABand` のような**繰り返し置ける部品**が必要で、
「ページに2つしか置けない特別なコンポーネント」ではない。

### 3-4. トーンとブランドの直交

変化は「自由度」ではなく「名前付きの選択肢」として渡す。締めすぎると全ページが
同じ顔になり、マーケティングとしては失敗するため。

| トーン     | 用途                                     | 性格                                                  |
| ---------- | ---------------------------------------- | ----------------------------------------------------- |
| `trust`    | コーポレートサイト、エンタープライズ向け | 明るい、余白広め、装飾最小、操作色のみ                |
| `product`  | サービスサイト                           | 明るい + アクセント、プロダクト画面主役、技術的       |
| `campaign` | 獲得LP、広告着地                         | コントラスト強め、ヒーロー濃色、CTA強調、装飾色を許可 |

**ブランド軸とトーン軸は直交する。**

- **ブランド**（`brand`）= 誰の顔か。色相・視覚デバイス・ロゴを決める
- **トーン**（`tone`）= 何を狙うページか。装飾量・コントラスト・余白量を決める

`<Page brand="peerdesk" tone="campaign">` は成立するし、
`<Page brand="corporate" tone="trust">` も成立する。
**組み合わせを禁止しない。** 一方が他方を上書きする関係にもしない。

`MarketingButton` の `gradient` バリアントは `campaign` トーンでのみ有効とする。

### 3-5. API 2本立て

用途が2つあるので、APIも2つ用意する。

#### (a) LP量産 — データ駆動

```tsx
<LandingPage {...defineLandingPage({
  pattern: 'product-portfolio-top',
  brand:   'peerdesk',
  tone:    'product',
  hero:    { title, subtitle, offers: [docOffer, priceOffer] },
  proof:   { logos } /* または { stats } */,
  products:[...],
  cases:   [...],
  closing: { form },
})} />
```

AIの仕事が「デザインする」から**「フォームを埋める」**に変わる。構成の判断が
発生しないので速く、間違えようがない。必須項目の欠落は**型エラーで落ちる**。

#### (b) コーポレート・サービスサイト — コンポジション

1ページずつ違うので、セクションを自由に並べられる形を残す。
Stage 1・2 が効いているため、並べ方だけ自由でリズムは自動。

```tsx
<Page brand="corporate" tone="trust">
  <Hero ... />
  <ModuleOverview ... />
  <SecurityBadges ... />
</Page>
```

Astro でコーポレートサイトとサービスサイトを作り、LPを量産するという計画に
この2本立てが対応する。

---

## Stage 4: リード獲得

Stage 3 までは「崩れないページが速く出る」までしか担保しない。
LPは**成果を測って改善する装置**であり、そのための口が現状ゼロ。

### 4-1. 計測

- 全CTAに `data-cta="<id>"` を出す
- `<Page onCTAClick={handler}>` でページ単位に一括で受け取る
- フォームの `onSubmit` / 送信成功 / 送信失敗をイベントとして出す
- **計測タグそのものは同梱しない。** GA4 も GTM も利用者の選択であり、
  DSがベンダーを決めてはならない。フックまでを提供する

### 4-2. 追従CTA — 2部品

当初の「モバイルは画面下固定バー」を**撤回する**。

| 観測                                               | 値                                   |
| -------------------------------------------------- | ------------------------------------ |
| 全幅の下部固定モバイルCTAバーを確認できたページ    | **0 / 19**（`[LP]`）                 |
| 固定ヘッダー内にCTA2本                             | 1件確認（カミナシ）                  |
| 右下フローティングカード                           | 1件確認（カミナシ）                  |
| 下部固定バーを「部品としては持つが当該LPで未使用」 | 1件（freee `.ncms-mod-sticky-bar2`） |

**ただしこれは「無い」ことの証明ではない。** SmartHR / Bill One / Sansan は
クライアントレンダリングで、JS実行後に挿入される可能性を排除できていない（`[LP]`）。
それでも**実装の根拠として使えるのは、実際に確認できた2形態だけ**である。

用意する部品は2つ。

| 部品                    | 実測値（カミナシ、`[LP]`）                                                                                                                                               |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`StickyHeaderCTA`**   | 固定ヘッダー内にCTAを2本内包。`position: fixed; top: 0; width: 100%; z-index: 2`。**モバイルでは2本が各 `45vw` / 高さ40px で横並び**、ハンバーガー40px、ヘッダー行 60px  |
| **`FloatingCornerCTA`** | 右下カード。`position: fixed; bottom: 30px; right: 30px; width: 380px; height: 240px; z-index: 5`。中身は2CTA。**閉じるボタン必須**。モバイルでは内部ボタン `width: 90%` |

`FloatingCornerCTA` の**dismiss（×）は任意ではなく必須にする。**
カミナシの実装が持っており、閉じられない追従要素はモバイルで本文を覆う。

全幅下部バーは実測が0件なので**作らない。** 必要になったら、そのとき根拠と一緒に足す。

### 4-3. 2オファー

Stage 2-D で定義した `offers: [Offer, Offer]` を、ページ型ごとの既定値として持つ。

| ページ型                            | 既定のオファー構成                              |
| ----------------------------------- | ----------------------------------------------- |
| `product` / `product-portfolio-top` | 軽（サービス資料）+ 軽（料金表） または 軽 + 重 |
| `lead-gen`                          | 単一オファー（そのページの資料）                |
| `corporate-top`                     | オファーなし                                    |

**FVのCTAは2本が圧倒的**（13/17 ≒ 76%、`[LP]`）。3本以上は dev 警告の対象にする。

### 4-4. フォームの摩擦

`[LP]` で項目数が実測できたのは freee会計のヒーロー内フォームのみで、
**メール1項目 + Google / Microsoft SSO の2ボタン**だった。他社は全て取得不可。

データが1件しかないので**項目数の上限を規則にはしない。** 代わりに構造で支える。

- `FormSection` は必須項目と任意項目を型で分ける
- 送信ボタンのラベルをオファー名と一致させる（「送信」ではなく「資料を受け取る」）
- 問い合わせ・見積もり・トライアルは**フォームを分ける**のが実態
  （SmartHR = 3本、Bill One = 2本、freee人事労務 = 3本。`[LP]`）。
  1つのフォームに用途セレクタを付ける設計は採らない

---

## Stage 5: 守り

### ページ単位のビジュアルリグレッション

Playwright のスクリーンショット比較をCIで回す（Chromatic は有料のため自前で十分）。
**検証単位はコンポーネントではなくページ。** 統一感はページを並べて初めて判定でき、
コンポーネント単位の Storybook では見えない。

**背景リズムの基準はここで作る。** 3-3 のとおり他社実態は未検証なので、
最初に自分たちが良いと判断したスナップショットが基準になる。
以降はそこからのズレを検出する。

### 日本語の最悪ケースを固定する

実務上いちばん効く。AIが書く日本語コピーは長さが暴れるため、見出しが3行に
折り返してヒーローが崩れる、カードの高さが揃わない、といった事故が最も多い。

「異常に長い和文」「異常に短い和文」「英語」の3パターンをページ単位のストーリーとして
持ち、VRTで固定する。**`text-wrap: balance`（Stage 1）の効果もここで確認する。**

### dev ビルドでの警告

| 警告                                     | 根拠                                                                  |
| ---------------------------------------- | --------------------------------------------------------------------- |
| `h1` が2つ以上ある                       | 構造の問題                                                            |
| プライマリCTAの**ラベル種類**が3種類以上 | `[LP]`。**回数は数えない**（当初の「1ビューポート2つ以上」は撤回）    |
| FVのCTAが3本以上                         | FV 2本が13/17（`[LP]`）                                               |
| 暗いセクションが3連続                    | 可読性                                                                |
| `StatBadge` に `asOf` が無い             | 景品表示法                                                            |
| 社会的証明スロットが空                   | ロゴ帯と数値バッジのどちらも無い状態。19/19が数値訴求を持つ（`[LP]`） |
| ロゴが1〜5社                             | 下記                                                                  |

**「ロゴ6社未満」の警告は撤回し、切替ガイドに置き換える。**

`[LP]` の実測では、**ロゴ帯と数値バッジは代替関係**にある。

| 選択                                          | ページ数          |
| --------------------------------------------- | ----------------- |
| **0社（ロゴ帯を置かず数値バッジに振り切る）** | **6 / 19（32%）** |
| 6〜8社（事例セクション内に少数）              | 2                 |
| 14社以上（カルーセル）                        | 5                 |

freee は「有料課金ユーザー企業数62万」「シェアNo.1 56.3%」の数値の圧倒性でロゴを不要にしており、
**0社は劣った選択ではない。** 「6社未満は逆効果」という因果は、CVRデータが非公開である以上
検証不能でもある。

→ **規則:**

1. 社会的証明スロットには「ロゴカルーセル」と「数値バッジ」の2実装があり、**どちらかを必ず置く**
2. ロゴを載せるなら**中央値は約20社**。14社以上でカルーセルにする
3. **1〜5社の中途半端なロゴ帯は日本語ページに実例が0件**。少数しか出せない場合は
   ロゴ帯ではなく**事例カードに紐付ける**（SmartHR 6社 / Bill One 8社の方式）

### 消費側からの結合テスト

最小の Astro アプリをCIでビルドし、生成物に期待するCSSが載っているかを検証する。
Storybook では絶対に見えない配布の事故を止める唯一の手段。

### CSSサイズの枠

`pnpm size` に CSS の枠を追加する。**ブランドを増やすとランプが増える**ため、
ブランド追加のたびにCSSが線形に膨らむ。ここに枠が無いと気づけない。

---

## Stage 6: AI向け規範ファイルの同梱

配布物（`files`）に機械可読のルールファイルを含める。CLAUDE.md はこのリポジトリの
中でしか効かず、`@siracusahq/gtm-design-system` を入れた先のAIエージェントには届かない。

Stage 1〜4 が完了していれば、書くことは劇的に減る。

```
ブランドは3つ。トーンは3つ。ページの型は5つ。あとは埋めるだけ。
import '@siracusahq/gtm-design-system/styles.css' を1行書く。
```

**APIで表現できるルールを文章で書くのは負け筋。** 現状のまま規範ファイルを書くと、
10兆通りの組み合わせのうち何が正しいかを文章で説明する羽目になり、守られない。

---

## リリース計画

> **実績（2026-08-06 追記）**: 実際の経過は本節の当初計画から次のとおりズレた。
> Stage 1〜2 完了時点（配布切替 = 利用者導線が固まった時点）で **`0.5.0`** として
> 公開済み（Version PR #121）。Stage 3 は追加のみ（パターン層）のため、完了時に
> **`0.6.0`（minor）** で公開する。「破壊的変更を一括で」の意図は 0.5.0 が担った。
> 以下の本文は当初計画の記録として残す。

**Stage 1〜3 をまとめて `0.4.0` として一括リリースする。破壊的変更。**

分割リリースにしない理由:

- Stage 1 だけ出すとトークン名が変わってスタイルが崩れ、Stage 2 だけ出すと
  props が消えてコンパイルが通らない。**どのみち利用者は一度書き直す**
- 破壊的変更を3回に分けると、利用者は3回書き直す
- 現在の利用者は社内のみ。**壊せる時期は今しかない**

Stage 4 以降は追加であり、`0.5.0` / `0.6.0` と minor で刻む。

changeset は Stage ごとに書くが、Version PR は Stage 3 完了時まで開かない。
Version PR を人が開く運用（ルート CLAUDE.md 参照）は、
**この破壊的変更を公開前に一度目視する機会**として機能する。

---

## 共有語彙の決定（2026-08-15・獲得系 Slice 0）

`acquisition-pages-workorder.md` §3-0 の指示による。**同じ概念に4つの型が生まれるのを
一度だけまとめて防ぐ**ためのスライスで、部品は1つも作らない。

### 前提の訂正: ContentHub は実装されていない

§3-0 は「ContentHub の実装中コード（別トラック）の現在のカード Props を読む」を
手順2に置いているが、**2026-08-15 時点で実体がどこにも無い**（`main` にも無く、
`agent/fable/content-types` は `main` より古く該当ファイルを持たない）。
突き合わせ相手が1つ欠けた状態での決定であることを明記しておく。

### 決定

| 判断項目               | 決定                                                                                                                                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **タクソノミー型**     | **統合しない。** 構造は同じでも意味する軸が違う（記事は年別 6/7・獲得系は年別 **0/15**）。実体の共有は**フィルタ UI（`SelectField` の列）** で既に足りている                                       |
| **画像型**             | **`ContentImage { src; alt }` に統合する。** `alt` 必須の契約は3系統共通で、統合しない理由が無い。TypeScript は構造的部分型なので、既存の `ArticleListItem.thumbnail` などはそのまま互換             |
| **ページネーション型** | **統合済み。** 記事系で `sections/pagination/` に切り出した `Pagination` を使う。ただし**資料一覧・セミナー一覧はページャを持たない**（実測）ため、獲得系では使わない                                |
| **カード4種**          | **統合しない（4部品のまま）。** 必須項目が系統ごとに違う — 事例=構造化メタ / 記事=公開日 / 資料=表紙・**日付を持たない** / セミナー=状態・日時。1型に寄せると全部 optional になり、「日付の無い記事」「状態の無いセミナー」が型で通る。`kind` 判別ユニオンで避けたのと同じ失敗になる |
| **ContentHub の語彙**  | **ContentHub は4系統のカードを union で受け取る。** 下の「撤回」を参照                                                                                                                              |
| **登壇者型**           | **`ContentPerson` を新設**し、セミナー登壇者に使う。`CaseSpeakerList`（最大4名のタプル）と `ArticlePerson`（著者・監修者）は構造的に互換なので**据え置く** — 改名は破壊的変更で、得られるものが名前の統一だけになる |

### 撤回: 「ContentHub のカード語彙を `ArticleListItem` に合わせる」

`article-pages-workorder.md` §2 のこの決定を**撤回する**。

理由は同 workorder §3-0 が自ら指摘していたとおりで、**獲得系のカードは日付を持たない**
（資料一覧 0/7。記事一覧は 14/14 が持つ）。`ArticleListItem` は `publishedAt` を必須に
しているため、資料カードをここへ寄せると必須項目を満たせない。逆に `publishedAt` を
optional に緩めると、記事側の「公開日は必ず出す」（News 12/12・ブログ 15/15）が
型で保証されなくなる。**どちらへ寄せても失うものの方が大きい。**

したがって ContentHub は「1つのカード型に統一する」のではなく、
**4系統のカードを受け取る union として設計する**こと。統合すべきなのは
カードではなく、その下の `ContentImage` のような小さな語彙である。

### 追記（2026-08-15・ContentHub 実測後）

上の「4系統」を **3系統 + News + 入口タイル**に改める。ContentHub の実ブラウザ計測
（16ページ。[content-hub-workorder.md](./content-hub-workorder.md) §9）で以下が分かったため。

- **事例（`case`）のハブ内出現は 0/17。** `sold-out` 0/29・`permanent` 1/21 を型に含めなかった
  前例に倣い、union から外す。**union へのメンバー追加は非破壊**なので、実測が採れたら足せる
- **News は表示形が構造的に違う**（サムネイル 0/7・日付 7/7 の行リスト）。語彙は
  `ArticleListItem` を共有し、枠の `kind` で表示だけを分ける
- **入口タイル**（個票ではなく一覧ページへ飛ぶ大型カード）が n=6 / 5社で実在した。
  既存4カードのどれとも語彙が違う（日付を持たず、説明文とボタン文言が標準装備）ため、
  ハブの枠種として持つ
- **バナー1枚形式（2/16 社）と外部メディア系統（1/16 社）は型に含めない。**
  実測が増えたら枠種として非破壊で追加できる

上の Slice 0 が「突き合わせ相手が1つ欠けた状態での決定」と留保していた、まさにその再判断にあたる。

**あわせて `[LP]` の「コンテンツ回遊 9/12」を訂正する。** 分母を再現できず、
製品系の日本語ページを列挙すると 13 本（保有9・非保有4）で **9/13** が正しい。
`research-lp-anatomy.md` は調査時点の記録なので書き換えず、
経緯は content-hub-workorder.md §9-1 を正本とする。**設計判断への影響は無い**
（9/12 = 75% も 9/13 = 69% も「多数が保有・ただし必須ではない」という結論を変えない）。

---

## 参考: 計測に使った環境

消費側の再現環境は Vite + `@tailwindcss/vite` の最小構成で組んだ。
Stage 5 の「消費側からの結合テスト」は、これを Astro ベースに組み直して流用する。

`[LP]` `[MB]` `[VL]` の3調査の生データ（取得したHTML/CSS、計算スクリプト、
取得失敗の記録）は、調査時のスクラッチパッドに残してある。
**各調査は「取得できなかったもの」を明示的に記録しており、
本文書で根拠として使っていないものは、そこに理由がある。**

再調査が必要な項目（`[LP]` `[VL]` が未決着と記録したもの）:

1. **背景の明暗リズム** — スクリーンショットが必要。Stage 5 で自基準を作るので、
   実装をブロックはしない
2. **モバイル下部固定CTAの実在** — レンダリング後DOMの取得が必要
3. **導入ロゴのグレースケール化** — 画像のCSS filter 確認が必要。`LogoMark` の既定値に影響する
4. **フォームの項目数** — freee会計（1項目）以外すべて取得不可
5. ~~**`case-study-detail`（個別事例記事）の構成** — 1本も取得できていない~~
   → **決着（2026-08-13）。** 実ブラウザ計測で 9 サイト × 3 記事を取得し、
   `case-study-detail` を6つ目のページ型として実装した（§3-1 の追記を参照）
6. **開発者向けインフラ／API系企業のビジュアル言語** — 調査対象に1社も含まれていない。
   Polastack のヒーローに図解・コードを置く判断の裏付けが、現状は「8社は非技術者向けだった」
   という消去法のみ
   → **Issue #165 として起票済み（2026-08-16）。** 実ブラウザ計測が可能になったため、
   調査の実行待ちの状態にある
7. ~~**ブログ記事の読み幅** — `[NB]` は CSS 実値を持たない~~
   → **決着（2026-08-15）。** 実ブラウザで 13本 / 5サイトを計測し、
   **`46.5rem` の流用を確定した**（全19本の中央値 730px との差 1.9%）。
   ブログ中央値 690px / News 中央値 780px で、**事例記事はちょうどその中間**にあった。
   種別ごとに読み幅を割らない判断も同時に確定している。
   本文の分量も想定と違い、実測は中央値 約5,500字（`[NB]` の「約7,000字」ではない）。
   経緯は [article-pages-workorder.md](./article-pages-workorder.md) §9-1 が正本
8. ~~**`[NB]` の分母の偏り** — 3社が取得できず News 12本のうち5本が2社に偏っている~~
   → **決着（2026-08-15）。3社とも取得できた。**
   News が 12本 → **18本**になり、偏りは 5/18 に薄まった。§2 の決定事項
   （「末尾 CTA を必須にしない」「著者 0/12」）を覆す材料は出ていない。
   **3社とも「存在しなかった」のではなくサブドメインの取り違えだった** —
   kintone の日本語ブログは `kintone-blog.cybozu.co.jp`（製品サイトのナビからは辿れない）、
   kubell は `www.kubell.com`（`corp.kubell.com` は今回も到達不可）。
   経緯は [article-pages-workorder.md](./article-pages-workorder.md) §9-2 が正本
9. **記事系ページの SP レイアウト** — `[NB]` は PC / SP の区別をしていない。
   ブログの目次（最大52項目）と著者ブロックが SP でどう畳まれるかは実装判断に直接効くが、実測が無い
10. ~~**資料ライブラリ（資料DL一覧）の構成** — 実測0~~
    → **決着（2026-08-14）。** `[RS]` で資料一覧7ページ・個票6本を実測し、
    **個票は既存 `lead-gen` で足りる**（ずれは `header` 1点のみ）ことを確認したうえで、
    一覧を `resources-library` として新設する判断に至った（§3-1 の追記②を参照）
11. ~~**セミナー / イベントの一覧・詳細の構成** — 実測0~~
    → **決着（2026-08-14）。** `[RS]` でセミナー一覧8ページ・詳細21本を実測し、
    `seminar-list` / `seminar-detail` を新設する判断に至った（§3-1 の追記②を参照）。
    **`article-list` の `kind` に足さなかったのは正しかった** — 獲得系は記事系の中核
    （章 `h2`・公開日・著者・目次・SNS シェア）を **0/27** で1つも持たない
12. ~~**「Bill One 型」（資料詳細ページを持たずフォーム直行）の未検証**~~
    → **決着（2026-08-15）。フォーム直行型は実在した。**
    Sansan（`jp.sansan.com/resources/service/`）と Bill One（`bill-one.com/resources/`）は
    **カードが全件 `/form/download_*` に直行**し、遷移先は入力欄 11〜13 のフォームだった。
    `[RS]` の「7/7 が詳細ページ経由」は**到達できなかった2社を分母に含めていなかった**ための結論。
    したがって **`ResourceListItem.href` は遷移先を固定しない**（詳細ページともフォームとも決めない）。
    なお ContentHub の実測でも同じ挙動が追認されている（ハブ内の資料カードは
    Bill One 3/3・Chatwork 3/3 がフォーム直行）。
    経緯は [acquisition-pages-workorder.md](./acquisition-pages-workorder.md) §9-1 が正本
13. **セミナーの「満席」表示** — `[RS]` は一覧8ページ・詳細21本を通じて
    満席・定員に達しましたの表示を **1件も採取できなかった**（0/29）。
    存在しないのか、調査時点でたまたま満席の回が無かったのかは切り分けられていない。
    `SeminarStatus` に `sold-out` を入れるかは実測が採れてから判断する（**非破壊で追加できる**）
14. **「常設セミナー」を型で表現していない** — 実測 1/21（kintone `/seminar/beginner/`。
    ANDPAD も一覧に「常設セミナー」タブを持つ）。**個別の回ではなく
    「シリーズの紹介ページ」**で、日時・登壇者・アジェンダ・フォームのいずれも持たず、
    `upcoming` / `closed` / `archive` のどれとも構造が違う。
    実測1本では専用の状態を配る根拠が無いため型から外したが、**状態としては実在する。**
    当面は `product` 系ページか `seminar-list` の入口として扱い、
    実測が増えたら `SeminarStatus` に非破壊で足すこと
15. **同一URLの「開催前 → 終了後」の時系列比較をしていない** — §3-1 追記②の
    「終了後の同一URLの姿」は、**終了済みのページと開催前のページを別々に取得して並べたもの**で、
    同じURLを時間差で2回取得したわけではない。結論（録画視聴への切替 0/9・資料DLへの切替 0/9）は
    複数サイトで一貫しているが、**同一ページの前後比較による裏付けは無い**
16. ~~**獲得系ページの読み幅** — `[RS]` は CSS 実値を持たない~~
    → **決着（2026-08-15）。** 実ブラウザで資料個票4本・セミナー詳細7本を計測し、
    **既存のコンテナ段をそのまま使うことを確定した**。資料個票の本文幅 513〜628px に対し
    `lead-gen` の `Container size="sm"`（640px）はレンジのすぐ上、
    セミナー詳細の中央値 660px に対し `Container size="md"` の実効内寸 704px は差 6.7%。
    **新しい寸法を作る根拠が無い。**
    経緯は [acquisition-pages-workorder.md](./acquisition-pages-workorder.md) §9-2 が正本
17. ~~**Sansan は3調査連続で本文に到達できていない**~~
    → **決着（2026-08-15）。到達できた。** News 記事3本と資料一覧の両方を取得し、
    #12 の決着に至った。前回「該当の記事がありません」を返した一覧が今回は正常に応答しており、
    JS 描画のタイミング差だったと考えられる。
    **教訓として残す: 「取得できなかった」は「存在しない」ではない。**
    今回 Sansan・kubell・kintone の3社がいずれも「存在しない」のではなく
    到達方法の問題だった（#8 も参照）
18. ~~**登壇者の型を `CaseSpeakerList` と共有できるか**~~
    → **決着（2026-08-15）。共有せず、`ContentPerson` を新設した**
    （`packages/ui-web/src/lib/content-vocabulary.ts`）。
    語彙が一致しているのは確認できたが、`CaseSpeakerList` は**最大4名のタプル**で
    「話者リスト」という別の制約を持っており、セミナー登壇者に流用すると制約まで持ち込む。
    かつ既存型の改名は破壊的変更で、**得られるものが名前の統一だけ**になる。
    TypeScript は構造的部分型なので `ArticlePerson` / `CaseSpeaker` とは相互に代入でき、
    実害は生じない。判断は末尾「共有語彙の決定」が正本
